# Merchant State Flow Remediation Log

Status: active handoff ledger
Created: 2026-05-31
Scope: merchant-side Mini Program/App state-flow audit repairs and adjacent high-risk fixes found during the audit loop

## Purpose

This file records fixes that were implemented after, or directly adjacent to, the merchant-side state-flow audit. It exists so a future context switch can answer three questions without reconstructing chat history:

1. Which audit finding was fixed?
2. Which commit contains the fix?
3. What risk remains after the fix?

When a future fix closes an audit finding, update this ledger, the parent row in `artifacts/merchant-state-flow-audit.md`, and the relevant slice under `artifacts/codegraph/merchant-state-flows/`.

## Repair Ledger

| Date | Flow | Commit | Risk | Fixed | Validation / Evidence | Remaining Risk |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-06-11 | `merchant-application-onboarding` | this commit | G3 | Closed explicit edit/reset queued-review supersede visibility. `ResetMerchantApplicationTx` now cancels active merchant onboarding review runs for the application as `run_status='cancelled'`, `outcome='needs_resubmit'`, and `reason_code='superseded_by_edit'` in the same reset transaction that moves the application back to `draft`; it refreshes `merchant_applications.review_summary` so merchant readers no longer see the stale queued state. The onboarding review worker now treats an existing merchant review run whose application is already non-submitted as a terminal superseded skip, records `needs_resubmit`, and refreshes `merchant_applications.review_summary`; if the reset transaction already cancelled the run, the worker treats no-row cancellation as idempotent convergence. | Red proof: `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run '^TestResetMerchantApplicationTxCancelsActiveReviewRuns$' -count=1 -v` first failed because the run stayed `queued`; `PATH="/usr/local/go/bin:$PATH" go test ./worker -run '^TestProcessTaskOnboardingReview_CancelsMerchantRunWhenApplicationSupersededByEdit$' -count=1 -v` first failed because the worker returned `merchant application ... status draft is not submitted`; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run '^TestCancelOnboardingReviewRunRecordsNeedsResubmitOutcome$' -count=1 -v` first failed because generic cancel left `outcome` empty; `PATH="/usr/local/go/bin:$PATH" go test ./logic -run '^TestMerchantOnboardingReviewServiceProcessSubmittedApplication_CancelSupersededRunUpdatesSummary$' -count=1 -v` first failed because the service did not refresh summary after cancel. Focused validation after fix: all red-proof commands passed; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run 'Test(ResetMerchantApplicationTxCancelsActiveReviewRuns\|ApproveMerchantApplicationTx\|CreateMerchantOnboardingReviewRun\|CancelOnboardingReviewRun)' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./worker -run '^TestProcessTaskOnboardingReview' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./logic -run 'Test(MerchantOnboardingReviewService\|OnboardingReviewService)' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run 'Test(UpdateMerchantApplicationBasicInfo\|ResetMerchantApplication\|SubmitMerchantApplication_(QueuesAsyncReviewWhenAvailable\|FallsBackToSyncReviewWhenEnqueueFails))' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestSubmitMerchantApplication_QueuesOnboardingReviewWhenAsyncAvailable$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -count=1`; `PATH="/usr/local/go/bin:$PATH" go test ./logic -count=1`; `PATH="/usr/local/go/bin:$PATH" go test ./worker -count=1`; `PATH="/usr/local/go/bin:$PATH" go test ./api -count=1`; `PATH="/usr/local/go/bin:$PATH" make sqlc`; `PATH="/usr/local/go/bin:$PATH" make check-generated`; `PATH="/usr/local/go/bin:$PATH" bash .github/scripts/test_backend_sql_guard.sh`; `git diff --check`. | Explicit edit/reset queued-review supersede is closed for reset transaction durability, visible summary, and worker race convergence. Remaining onboarding gaps are stale pending OCR cleanup, submit retry idempotency with existing review runs, approved edit product semantics, and failed-validation consent audit semantics. |
| 2026-06-11 | `merchant-application-onboarding` | this commit | G3 | Closed the mutating submitted-application GET race. `GET /v1/merchant/application` now returns an existing `submitted` application without calling `ResetMerchantApplicationTx`, so opening the merchant application page while async onboarding review is queued no longer silently changes the application back to `draft`. Explicit write paths still use `checkApplicationEditable` and reset submitted/approved applications to draft only after edit intent. | Red proof: `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestGetOrCreateMerchantApplicationDraft$/^GetExisting_SubmittedDoesNotReset$' -count=1 -v` first failed because the handler called `ResetMerchantApplicationTx` on GET. Focused validation after fix: the same red proof command passed; `PATH="/usr/local/go/bin:$PATH" go test ./api -run 'Test(GetOrCreateMerchantApplicationDraft|UpdateMerchantBasicInfo|ResetMerchantApplication|GetMyMerchantApplicationDoesNotResetSubmittedApplication)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestUpdateMerchantApplicationBasicInfo$/(Submitted_AutoResetToDraft|Approved_AutoResetToDraft)$' -count=1 -v`. | Submitted page-open reset is closed. The later 2026-06-11 row closes explicit edit/reset queued-review supersede visibility. Remaining onboarding gaps are stale pending OCR cleanup, submit retry idempotency with existing review runs, approved edit product semantics, and failed-validation consent audit semantics. |
| 2026-06-11 | `merchant-app-bind-and-device` | this commit | G2 | Closed per-device native-push terminal-failure degradation. Migration `000266` adds `push_failure_count`, `last_push_failure_reason`, `last_push_failure_at`, and `push_degraded_at` to `merchant_app_devices`. `RecordMerchantAppDevicePermanentPushFailure` records sanitized provider reason/count on only the affected active row and current push token, keeps the row active for the first two terminal failures, and deactivates only that row after the third consecutive terminal failure. `ClearMerchantAppDevicePushFailure`, successful dispatcher sends, registration, and heartbeat clear stale degradation evidence. Retryable failures and unconfigured providers do not mutate durable device state. | Red proof: `PATH="/usr/local/go/bin:$PATH" go test ./logic -run 'TestMerchantAppPushDispatcher(Dispatch\|ClearsDeviceDegradationAfterSuccessfulSend)$' -count=1 -v` first failed because the store mock lacked `RecordMerchantAppDevicePermanentPushFailure` / `ClearMerchantAppDevicePushFailure` and `MerchantAppDevice` lacked push-failure fields; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run 'Test(RecordMerchantAppDevicePermanentPushFailure\|ClearMerchantAppDevicePushFailure)' -count=1 -v` first failed because the Store/query/types did not exist. Focused validation after fix: `PATH="/usr/local/go/bin:$PATH" go test ./logic -run 'TestMerchantAppPushDispatcher(Dispatch\|ClearsDeviceDegradationAfterSuccessfulSend)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run 'Test(MerchantAppDevicePushDegradationMigration\|RecordMerchantAppDevicePermanentPushFailure\|ClearMerchantAppDevicePushFailure\|UpdateMerchantAppDeviceHeartbeatClearsPushFailure)' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run 'Test.*MerchantAppDevice.*' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./scheduler -run 'TestDataCleanupScheduler_(CleanupStaleMerchantAppDevices\|StaleMerchantAppDeviceCleanupSchedule)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./logic -count=1`; `PATH="/usr/local/go/bin:$PATH" make sqlc`; `PATH="/usr/local/go/bin:$PATH" make check-generated`; `PATH="/usr/local/go/bin:$PATH" bash .github/scripts/test_backend_sql_guard.sh`; `git diff --check`. | Per-device terminal-failure degradation is closed for backend dispatcher/persistence and migration clean/incremental schema proof. Verify-time device metadata remains log-only by design; durable device truth remains `/v1/merchant/device/register`. |
| 2026-06-11 | `merchant-app-bind-and-device` | this commit | G2 | Closed backend stale-device cleanup for merchant App devices that never receive active logout. `DataCleanupScheduler` now runs a daily cleanup that deactivates active `merchant_app_devices` rows whose `last_active_at` is older than the 90-day no-heartbeat cutoff; the cleanup preserves durable rows for audit/history and only changes the affected device row, not the merchant account, staff user, or App-wide push capability. Migration `000265` adds an active-device `last_active_at` partial index for the scheduler scan. | Red proof: `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run '^TestDeactivateStaleMerchantAppDevices$' -count=1 -v` first failed because `DeactivateStaleMerchantAppDevices` did not exist; `PATH="/usr/local/go/bin:$PATH" go test ./scheduler -run '^TestDataCleanupScheduler_CleanupStaleMerchantAppDevices$' -count=1 -v` first failed because the store mock and scheduler cleanup method did not exist. Focused validation after fix: `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run 'Test(DeactivateStaleMerchantAppDevices\|MerchantAppDeviceStaleCleanupIndexExists\|MerchantAppDeviceStaleCleanupIndexMigrationCleanDatabaseContract\|MerchantAppDeviceStaleCleanupIndexMigrationIncrementalContract)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./scheduler -run 'TestDataCleanupScheduler_(CleanupStaleMerchantAppDevices\|StaleMerchantAppDeviceCleanupSchedule)$' -count=1 -v`; adjacent validation covered scheduler, merchant App device API/logic, push dispatcher, sqlc generation, generated artifact checks, and SQL guard. | Backend stale-device cleanup is closed for uninstall/storage-wipe/no-logout convergence after 90 days without heartbeat. The later 2026-06-11 row closes the separate per-device native-push degradation policy. |
| 2026-06-11 | `merchant-app-bind-and-device` | this commit | G2 | Closed the active Flutter logout unregister gap. `DeviceSyncService.unregisterCurrentDevice()` now deletes the current backend device only when a local `device_uuid` exists, clears the local last-registered push-token marker in both success and failure cases, and lets the next bind/login force registration for the same native push token. `AuthLogoutController` coordinates active logout from the order drawer and settings page by attempting device unregister before clearing local auth, while keeping logout successful when unregister fails. | Red proof: `PATH="$HOME/.local/bin:$PATH" flutter test test/device_sync_service_test.dart` first failed because `unregisterCurrentDevice()` did not exist, then a missing-device-id test failed because the implementation tried `DELETE /merchant/device/`. `PATH="$HOME/.local/bin:$PATH" flutter test test/auth_notifier_test.dart` first failed because `AuthLogoutController` did not exist, then proved unregister failure must not block token clearing. Focused validation after fix: `PATH="$HOME/.local/bin:$PATH" flutter test test/device_sync_service_test.dart`; `PATH="$HOME/.local/bin:$PATH" flutter test test/auth_notifier_test.dart`; `PATH="$HOME/.local/bin:$PATH" flutter analyze`. | Active logout unregister is closed. A later 2026-06-11 backend row closes uninstall/storage-wipe/no-logout stale-device cleanup. Remaining App bind/device work is separate: per-device native-push degradation without affecting the merchant account, staff user, or App-wide push capability. |
| 2026-06-11 | `merchant-app-bind-and-device` | this commit | G2 | Closed the Flutter device-sync platform contract drift. Backend `merchant_app_devices.platform`, logic validation, and the DB check are Android-only; `DeviceSyncService` now normalizes registration and heartbeat payloads to backend platform `android` even when the runtime platform resolver observes a non-Android branch for local metadata. | Red proof: `PATH="$HOME/.local/bin:$PATH" flutter test test/device_sync_service_test.dart` failed before implementation because the new contract test could not inject a non-Android runtime platform, while the existing payload builder derived `ios/web` directly from runtime state. Focused validation after fix: `PATH="$HOME/.local/bin:$PATH" flutter test test/device_sync_service_test.dart`, covering registration payload platform, non-Android runtime normalization to backend `android` for both register and heartbeat, push-token missing diagnostics, registration failure diagnostics, native-push initialization failure diagnostics, and heartbeat recovery state. | Android-only Flutter platform payload is closed. Later 2026-06-11 rows close active logout unregister and backend stale-device cleanup for cases where logout never runs. Remaining App bind/device work is separate: per-device native-push degradation without affecting the merchant account, staff user, or App-wide push capability. |
| 2026-06-11 | `merchant-app-bind-and-device` | this commit | G3 | Closed the App bind-code consume-after-recheckable-preconditions decision. Public verify now keeps Redis `app_bind:<code>` and `app_bind:user:<user>` valid through merchant-role recheck, user lookup, access/refresh token creation, token hashing, and access-profile construction. The final consume uses an atomic Redis compare-and-delete so duplicate successful consumption still fails closed; if session creation fails after final consume, the handler attempts to restore the code with the consumed key's remaining millisecond TTL, skips restoration when the user index already points at a newer generated code, and does not revive already-expired/no-TTL consumption results. Changed-role/business invalidation still consumes the code before returning forbidden. | Red proof: `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestVerifyAppBindCodeDoesNotConsumeCodeWhenUserLookupFails$' -count=1 -v` failed before the implementation change because the Redis code/user index were deleted before `GetUser`. Focused validation after fix: `PATH="/usr/local/go/bin:$PATH" go test ./api -run 'Test.*AppBindCode\|TestGenerateAppBindCode\|TestVerifyAppBindCode\|TestRestoreConsumedAppBindCode.*' -count=1 -v`, covering Redis unavailable, changed merchant role consumption, no code burn on user lookup failure, generate -> verify -> one-time reuse denial, stale user-index regeneration, session creation failure restoration, no overwrite of newer user-index code, and no expired-code revival. | App bind-code consumption order is closed for backend verify. Later 2026-06-11 rows close Flutter platform payload, active logout unregister, and backend stale-device cleanup for cases where logout never runs. Remaining App bind/device work is separate: per-device native-push degradation without affecting the merchant account, staff user, or App-wide push capability. |
| 2026-06-11 | `merchant-reservation-and-table` | this commit | G3 | Closed the remaining offline/phone operator-as-customer authorization gap after the 2026-06-10 identity/no-show repair. Customer-owner reservation checks now only recognize `NULL`, blank, trim-space, or `online` sources; phone/walk-in/merchant reservations still keep the operator in legacy `table_reservations.user_id` for compatibility, but that value no longer authorizes customer detail, cancel, check-in, start-cooking, dish adjustment, reservation payment, order-session, dining-session/precheck, replacement-order, or transaction-level payment/adjustment paths. `/v1/reservations/me` list/count now share the same source filter so offline operator rows do not inflate customer totals. | Focused validation: `go test ./logic -run 'Test(CancelReservationRejectsOfflineOperatorAsCustomerOwner|StartCookingReservationRejectsOfflineOperatorAsCustomerOwner|AddReservationDishesRejectsOfflineOperatorAsCustomerOwner|ModifyReservationDishesRejectsOfflineOperatorAsCustomerOwner|ValidateOrderSessionAndBilling_(Reservation|DineIn)|PaymentOrderServiceCreateReservationPaymentRejectsOfflineOperatorAsCustomerOwner)' -count=1 -v`; `go test ./api -run 'Test(GetReservationAPI|ListUserReservationsAPI)' -count=1 -v`; `go test ./db/sqlc -run 'TestCountReservationsByUserAndStatusExcludesOfflineOperatorReservations' -count=1 -v`; broader focused validation also covered reservation API actions, reservation logic paths, `PrecheckDiningSession`, payment-order service, and the existing `000264`/no-show/migration sqlc tests. Final generated checks and SQL guard are recorded with this commit. | Offline/phone identity, no-show non-attribution, and customer-owner authorization/count closure are now one model: `NULL`, blank, trim-space, or `online` rows can be customer-owned, non-online rows require merchant authorization. Remaining reservation/table work is no longer this identity/authz item; only separate optional product choices such as terminal action idempotent replay should be considered if requested. |
| 2026-06-10 | `merchant-reservation-and-table` | this commit | G3 | Closed the offline/phone customer identity and no-show attribution gap. Migration `000264` adds `merchant_offline_customers`, `table_reservations.offline_customer_id`, and `table_reservations.created_by_user_id`, trims historical non-online contact fields before grouping, normalizes blank historical contact names to an offline-customer fallback, backfills by merchant/contact phone, preserves earliest `first_seen_at` and latest `last_seen_at`, links old reservations to the offline identity, and adds the FK support index `table_reservations_offline_customer_id_idx`. `CreateMerchantReservationTx` now upserts the offline customer and links new merchant-created reservations while keeping legacy `table_reservations.user_id` as the operator compatibility field; `UpdateReservationTx` re-upserts and repoints `offline_customer_id` when offline contact name/phone changes. `GetMerchantOfflineCustomer` is merchant-scoped because rows contain contact PII. `MarkNoShowTx` leaves `behavior_decisions.user_id` null for non-online sources and writes `customer_identity_type`, `offline_customer_id`, `created_by_user_id`, and `reservation_source` into `fact_snapshot`; online reservations still attribute no-show to the online user. Merchant-created reservation input trims and validates contact name/phone/source before table locking or DB writes. | Red proof: `go test ./db/sqlc -run TestMarkNoShowTxDoesNotAttributeMerchantCreatedReservationToOperator -count=1 -v` failed before the transaction change because no-show still wrote the operator user id; `go test ./db/sqlc -run TestMerchantOfflineCustomerMigration264BackfillsHistoricalReservationIdentity -count=1 -v` failed before migration correction because `first_seen_at` used the latest historical reservation. Focused validation after fix: `go test ./db/sqlc -run 'Test(UpdateReservationTxRepointsOfflineCustomerWhenContactPhoneChanges|MerchantOfflineCustomerMigration264BackfillsHistoricalReservationIdentity)' -count=1 -v`; `go test ./db/sqlc -run 'Test(MarkNoShowTx|CreateMerchantReservationTx|MerchantReservationEditOpenAndCloseDiningSessionFlow|MerchantOfflineCustomerMigration264|UpdateReservationTxRepointsOfflineCustomerWhenContactPhoneChanges)' -count=1 -v`; `go test ./api -run 'TestMerchant(Create|Update)ReservationAPI(RejectsBlankContactPhone|Success|RejectsDisabledTableAfterLock|RejectsDeletedTableAfterLock|SuccessUsesTransaction|RejectsDisabledTargetTableAfterLock|RejectsDeletedTargetTableAfterLock)' -count=1 -v`; `go test ./logic -run 'Test.*Reservation' -count=1 -v`; `make sqlc`; `make check-generated`; SQL guard and final review are recorded with this commit. | Offline/phone customer identity and no-show non-attribution are closed for migration backfill, contact normalization, blank-name cleanup, merchant-scoped PII lookup, new merchant-created reservations, offline contact edits, sqlc transaction behavior, logic validation, and API failure mapping. Remaining reservation/table work is no longer this identity item; only separate optional product choices such as terminal action idempotent replay should be considered if requested. |
| 2026-06-10 | `merchant-staff-and-group` | this commit | G3 | Closed staff invite-code revoke/rotate with immediate invalidation. `POST /v1/merchant/staff/invite-code` remains a stable get-or-generate popup path that reuses an active code and conditionally creates a new code only when no active code exists, re-reading the current active code when a concurrent popup already created one. New explicit actions `POST /v1/merchant/staff/invite-code/rotate` and `POST /v1/merchant/staff/invite-code/revoke` overwrite or clear the single current `merchants.bind_code` credential, so old codes fail immediately at `POST /v1/bind-merchant`. Migration `000263` cleans historical duplicate stored bind codes on non-deleted merchants and adds `merchants_active_bind_code_uidx`; API generation retries on `23505` unique collisions so an invite code cannot drift across merchants through random collision. The Mini Program staff invite popup now exposes copy, rotate, revoke, stopped-state regenerate, and duplicate-action guards. | Red proof: focused API tests first failed because rotate was missing and revoke conflicted with the `/:id` delete route shape; `TestRotateInviteCodeRetriesOnBindCodeCollision` then failed with HTTP 500 before collision retry was added; `TestGenerateInviteCodeStaleSnapshotReusesCodeCreatedByConcurrentRequest` failed before conditional create/current-code re-read because a second cold popup could overwrite and return a different code. Focused validation after fix: `go test ./api -run 'Test(GenerateInviteCodeReusesExistingActiveCode\|GenerateInviteCodeStaleSnapshotReusesCodeCreatedByConcurrentRequest\|RotateInviteCodeReplacesExistingActiveCode\|RotateInviteCodeRetriesOnBindCodeCollision\|RevokeInviteCodeClearsCurrentCode\|BindMerchantRejectsRevokedInviteCode)$' -count=1 -v`; `go test ./db/sqlc -run 'Test(UpdateMerchantBindCodeRotationInvalidatesOldCode\|CreateMerchantBindCodeWhenInactiveDoesNotOverwriteActiveCode\|UpdateMerchantBindCodeRejectsDuplicateActiveCode\|UpdateMerchantBindCodeRevokeInvalidatesOldCode\|MerchantStaffInviteCodeMigration263CleansDuplicateActiveCodes)$' -count=1 -v`. Final validation for this commit also covers adjacent staff invite/staff auth tests, Swagger regeneration, generated-artifact check, Mini Program compile/lint/WXML handler checks, review, and `git diff --check`. | Staff invite-code revoke/rotate is closed for backend route, cold get-or-generate concurrency, persistence lookup, global stored-code uniqueness, bind rejection, and traced Mini Program staff invite UI. Remaining staff/group follow-ups are separate group-contract drift candidates such as group review route alias cleanup or applicant-side group-join cancellation UI if product wants it. |
| 2026-06-10 | `merchant-staff-and-group` | this commit | G3 | Closed disabled/removed staff re-application and owner-confirmed reactivation. `POST /v1/bind-merchant` now allows an existing disabled `merchant_staff` row to re-enter through a valid invite as the same row with `role='pending', status='active'`, while keeping coarse `user_roles.merchant_staff` disabled unless another assigned active staff role exists. Owner direct-add now allows a disabled historical row to be reactivated with the owner-confirmed assigned role and reactivates the coarse role in the same transaction. Active/pending duplicate bind/add attempts still return conflict, and the disabled-row path is locked in `AddMerchantStaffTx` before mutation. | Red/focused validation: `go test ./api -run 'Test(BindMerchantReappliesDisabledStaffAsPending\|AddMerchantStaffAPIReactivatesDisabledStaffWithOwnerConfirmedRole)$' -count=1 -v`; `go test ./db/sqlc -run 'TestAddMerchantStaffTxReactivatesDisabledStaff(AsPendingWithoutCoarseRole\|WithAssignedRole)$' -count=1 -v`; adjacent validation: `go test ./db/sqlc -run 'Test(AddMerchantStaffTx\|AssignMerchantStaffRoleTx\|RemoveMerchantStaffTx\|CheckUserHasMerchantAccess\|CountMerchantStaff\|GetUserMerchantRole)' -count=1 -v`; `go test ./api -run 'Test(BindMerchantDoesNotGrantMerchantStaffRoleWhenPending\|BindMerchantReappliesDisabledStaffAsPending\|BindMerchantRejectsExistingActiveOrPendingStaff\|AddMerchantStaffAPIUsesAtomicStaffRoleTx\|AddMerchantStaffAPIReactivatesDisabledStaffWithOwnerConfirmedRole\|AddMerchantStaffAPIRejectsExistingActiveOrPendingStaff\|UpdateMerchantStaffRoleAPIUsesAtomicStaffRoleTx\|DeleteMerchantStaffAPIUsesAtomicStaffRoleTx)' -count=1 -v`; `make sqlc`; `make check-generated`; `git diff --check`. | Disabled-staff re-application/owner-confirmed reactivation is closed for the traced backend invite and owner direct-add paths. The 2026-06-10 staff invite-code row above closes the former revoke/rotate remaining risk. |
| 2026-06-10 | `merchant-staff-and-group` | this commit | G3 | Closed group application minimum-document completeness. Backend group submit now rejects draft submission before consent audit/state transition unless the application has a business license, legal representative ID card front/back, and a registered trademark certificate. `PUT /v1/groups/applications/basic` accepts and validates `trademark_certificate_asset_id` through the private `group_trademark_certificate` media category, response DTOs expose the asset id, document deletion supports `trademark_certificate`, and SQLC merges/removes the trademark certificate key without dropping sibling OCR/document JSON. Both merchant-console and register Mini Program group-application entries now upload the trademark certificate, mark business license/trademark as required, preview private media, delete the certificate, and require persisted backend asset ids before advancing. | Red/focused validation: `go test ./api -run 'Test(UpdateGroupApplicationBasicAPI\|SubmitGroupApplicationAPI\|DeleteGroupApplicationDocumentAPI)' -count=1 -v`; `go test ./db/sqlc -run 'TestUpdateGroupApplication(LicenseMergesApplicationDataPatch\|BasicMergesAndClearsTrademarkCertificatePatch)' -count=1 -v`; `go test ./media -run TestPolicy -count=1 -v`; `make swagger`; `make check-generated`; Mini Program `npx tsc --noEmit --pretty false`; `npm run compile`; `npm run lint`. | Group submit minimum-document completeness is closed for the HTTP submit path and both traced Mini Program applicant entries. The 2026-06-10 staff invite-code row above closes the former revoke/rotate remaining risk. |
| 2026-06-10 | `merchant-device-display-config` | this commit | G2 | Closed Flutter App auto-accept convergence to the backend display-config truth. The App now reads `GET /v1/merchant/display-config` before alert-driven auto-accept and only calls the merchant accept endpoint when the effective backend config allows `enable_print && auto_accept_paid_orders`; config read failures or timeouts fail closed and continue the ordinary alert/pending flow. The App settings page no longer exposes a local auto-accept preference, and legacy local `order_auto_accept_enabled` preferences are no longer read. | Red proof: `flutter test test/order_alert_coordinator_test.dart` first failed because there was no App display-config provider/repository contract and the order alert coordinator still used the local setting. Focused validation after fix: `flutter test test/display_config_provider_test.dart test/order_alert_coordinator_test.dart test/order_poller_test.dart`; final Flutter validation: `flutter analyze`; `flutter test`; workspace whitespace check: `git diff --check`. | App alert-driven auto-accept now follows one backend truth. Remaining device/display follow-ups: merchant-facing reconciliation UI or wrapper cleanup and Flutter BLE backend-config/observability/dedupe implementation. |
| 2026-06-10 | `merchant-claim-recovery` | this commit | G3 | Closed the product-approved manager recovery-dispute create permission proof without changing production behavior. `TestCreateMerchantRecoveryDisputeAPIManagerCanCreate` now exercises the registered `POST /v1/merchant/recovery-disputes` route with a staff manager, proves the owner/manager recovery middleware admits the request, and verifies the create transaction receives the merchant appellant, claim id, reason, recovery target, and region. Existing authz proof continues to deny cashiers before recovery lookup and deny manager recovery payment through the owner-only payment route. | Red proof: the new manager test failed with HTTP 403 and no transaction call when the recovery-dispute route was temporarily narrowed to owner-only, then passed after restoring the product-approved owner/manager middleware. Focused validation: `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^(TestCreateMerchantRecoveryDisputeAPIManagerCanCreate|TestMerchantClaimRecoveryRoutesDenyLowPrivilegeStaff)$' -count=1 -v`; adjacent recovery-dispute validation: `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestCreateMerchantRecoveryDisputeAPI' -count=1 -v`; workspace whitespace check: `git diff --check`. | Manager dispute-create permission and low-privilege/payment denial proof are closed. Remaining claim-recovery follow-ups are separate: decide whether external clients need a claim-id compatibility recovery route, and expose merchant-facing delayed-release visibility when paid/waived release action execution is still pending. |
| 2026-06-10 | `merchant-device-display-config` | this commit | G2 | Closed display-config `enable_print`/`auto_accept_paid_orders` normalization. Backend `PUT /v1/merchant/display-config` now forces `auto_accept_paid_orders=false` before create/update whenever the effective `enable_print` value is false, including partial updates where the existing row already has printing disabled. Migration `000262` cleans historical rows with `enable_print=false AND auto_accept_paid_orders=true` and adds `order_display_configs_print_auto_accept_check`, so direct SQL/sqlc writes cannot reintroduce that stored contradiction; stale API writes that hit this DB CHECK now return a stable 409 conflict instead of a raw server error. The Mini Program display-config page now clears the local auto-accept draft when print is switched off, defensively submits auto-accept as false while print is disabled, and updates copy so disabling print clearly disables auto-accept too. | Red proof recorded during this repair: `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestUpdateDisplayConfigAPI/(OK_UpdateExisting\|OK_UpdateExistingPrintAlreadyDisabledForcesAutoAcceptOff\|OK_CreateNewWithPrintDisabledForcesAutoAcceptOff)$' -count=1 -v` failed before backend normalization because true auto-accept reached the store; `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestUpdateDisplayConfigAPI/Conflict_DBRejectsPrintDisabledAutoAcceptDrift$' -count=1 -v` failed before API conflict mapping because the DB CHECK surfaced as HTTP 500; `npm run check:merchant-display-config-auto-accept` failed before Mini Program draft/payload normalization; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run '^TestOrderDisplayConfigRejectsAutoAcceptWhenPrintDisabled$' -count=1 -v` failed before DB constraint because direct sqlc create accepted the dirty combination. Focused validation: `npm run check:merchant-display-config-auto-accept`; `npm run compile`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^Test(UpdateDisplayConfigAPI\|GetDisplayConfigAPI)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run '^TestOrderDisplayConfig(RejectsAutoAcceptWhenPrintDisabled\|AutoAcceptMigration(CleanDatabaseContract\|262CleansHistoricalDirtyRows))$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" make swagger`; `PATH="/usr/local/go/bin:$PATH" make check-generated`; `PATH="/usr/local/go/bin:$PATH" bash .github/scripts/test_backend_sql_guard.sh`; `git diff --check`. | Display-config print-disabled/auto-accept contradiction is closed for backend writes, direct DB writes, historical stored data, API stale-conflict mapping, and the Mini Program page. Remaining device/display follow-ups: merchant-facing reconciliation UI or wrapper cleanup and Flutter BLE backend-config/observability/dedupe implementation. |
| 2026-06-10 | `merchant-device-display-config` | this commit | G2 | Closed the Mini Program voice-control removal and backend compatibility/no-op contract. The display-config page form and save payload no longer track or submit `enable_voice`, `voice_takeout`, or `voice_dine_in`; the page no longer renders voice switches; the config entry copy now advertises print dispatch and auto-accept rather than voice broadcast. Backend `GET /v1/merchant/display-config` still returns stored/default `voice_*` fields for old clients, and `PUT /v1/merchant/display-config` still accepts legacy request fields, but create keeps default compatibility values and update leaves existing persisted voice fields unchanged. | Red proof recorded during this repair: `npm run check:merchant-display-config-auto-accept` failed before UI removal on deprecated Mini Program voice controls; `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestUpdateDisplayConfigAPI/(OK_UpdateExisting|OK_CreateNew)$' -count=1 -v` failed before backend no-op handling because legacy `voice_*` requests reached write params. Focused validation: `npm run check:merchant-display-config-auto-accept`; `npm run compile`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^Test(UpdateDisplayConfigAPI|GetDisplayConfigAPI)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" make swagger`; `PATH="/usr/local/go/bin:$PATH" make check-generated`; `git diff --check`. | Mini Program voice controls and backend `voice_*` PUT no-op compatibility are closed. Remaining device/display follow-ups: merchant-facing reconciliation UI or wrapper cleanup and Flutter BLE backend-config/observability/dedupe implementation. |
| 2026-06-10 | `merchant-member-balance-adjust` | this commit | G3 | Retired unused generated direct balance SQL writers. `IncrementMembershipBalance` and `DecrementMembershipBalance` had no runtime call sites and bypassed the richer ledger/idempotency semantics of `RechargeTx`, `ConsumeTx`, `AdjustMemberBalanceTx`, order deduction, and cancellation rollback. The queries were removed from SQL source, generated sqlc, `Querier`, and `MockStore`; list tests now seed balance state through transaction helpers instead of the retired direct writer surface. | Red proof: `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run TestMembershipBalanceDirectWritersRetired -count=1` failed while the SQL source still exposed `IncrementMembershipBalance`. Focused validation after fix: `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run 'Test(MembershipBalanceDirectWritersRetired\|UpdateMembershipBalance\|ListUserMemberships\|ListMerchantMembers\|RechargeTx\|ConsumeTx\|ConsumeTxInsufficientBalance\|ConsumeTxExactBalance\|AdjustMemberBalanceTxRequiresIdempotencyKey\|AdjustMemberBalanceTxReplaysSameIdempotencyKey\|AdjustMemberBalanceTxRejectsConflictingIdempotencyKey)$' -count=1`; `PATH="/usr/local/go/bin:$PATH" make sqlc`. | Known member-balance repair items are closed. Optional future product/API work remains: whether manual adjustment responses should expose a transaction id as a durable audit anchor. |
| 2026-06-10 | `merchant-member-balance-adjust` | this commit | G3 | Closed the product-approved manager manual stored-value mutation proof without changing production behavior. `TestAdjustMemberBalanceAPIManagerCanAdjust` now exercises the registered `POST /v1/merchants/:id/members/:user_id/balance` route with a staff manager, proves `MerchantStaffMiddleware("owner","manager")` admits the request, and verifies the adjustment reaches `AdjustMemberBalanceTx` with the target membership, signed amount, notes, and trimmed `Idempotency-Key`. `TestAdjustMemberBalanceAPICashierDenied` proves lower-privilege cashier staff are rejected before membership lookup or balance transaction calls. Red check was verified by temporarily narrowing the route to owner-only; the manager test failed with 403 and no transaction call, then passed after restoring the production route. | Focused validation: `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestAdjustMemberBalanceAPI(ManagerCanAdjust\|CashierDenied\|ForwardsIdempotencyKey\|RequiresIdempotencyKey)$' -count=1 -v`. Red proof: the same manager test failed under a temporary owner-only route with HTTP 403 before the route was restored. | Manager write permission and lower-privilege denial proof are closed. The former direct generated balance SQL writer cleanup is closed by the adjacent 2026-06-10 row. |
| 2026-06-10 | `merchant-finance-withdrawal` | this commit | G3 | Closed the Mini Program accepted/unknown -> durable detail -> terminal refresh proof without changing production behavior. `check:baofu-withdrawal-workflow` now loads the merchant withdrawal create/detail pages in a runtime harness and proves the create page blocks duplicate in-flight submit, sends the stable draft `Idempotency-Key`, redirects using the backend-returned durable withdrawal id, and the detail page reloads that id, polls non-terminal status while visible, refreshes to terminal truth, and clears polling. | Focused validation: `npm run check:baofu-withdrawal-workflow`. | Remaining withdrawal risk is now the pre-existing local `processing` row before provider create call: a process crash before the provider request can leave a row that recovery may not find provider-side unless the flow is redesigned around an outbox/command-send boundary. |
| 2026-06-10 | `merchant-finance-withdrawal` | this commit | G3 | Closed the provider withdrawable-balance versus local finance-report reconciliation proof without changing production behavior. Added an API contract test proving finance overview uses local settlement/promotion/adjustment truth and does not call or expose Baofu balance fields, while withdrawal balance uses the active Baofu binding, calls provider `QueryBalance`, returns provider amount fields, and does not expose local finance totals. | Focused validation: `PATH="/usr/local/go/bin:$PATH" go test ./api -run '^TestMerchantFinanceOverviewAndBaofuWithdrawalBalanceUseSeparateTruthSources$' -count=1 -v`. | Remaining withdrawal risk is now the pre-existing local `processing` row before provider create call: a process crash before the provider request can leave a row that recovery may not find provider-side unless the flow is redesigned around an outbox/command-send boundary. |
| 2026-06-10 | `merchant-finance-withdrawal` | this commit | G3 | Closed the 2026-06-10 withdrawal idempotency, manager operation, callback-fact, provider-error diagnostic, and applied-schema hardening decisions. Backend withdrawal create now requires `Idempotency-Key`, stores `idempotency_key` plus `idempotency_request_hash` on `baofu_withdrawal_orders`, adds a per-owner partial unique index, replays same owner/key/amount as HTTP 200 without a provider create call, rejects same owner/key/different amount as 409, and rejects half-written key/hash pairs at the DB boundary. Migration `000261` forward-repairs environments that already applied a weaker `000260` CHECK. Merchant managers can create withdrawals through the finance owner/manager route group while settlement-account submission remains owner-only. Mini Program merchant/operator/platform/rider withdrawal wrappers and create pages now require/send a stable per-draft idempotency key. Baofu withdrawal callbacks now persist an `external_payment_facts` callback fact before enqueue/ACK, and fact persistence failure returns callback failure without enqueuing terminal application. Baofu withdrawal create provider errors now preserve upstream code, classified/sanitized command message, and sanitized diagnostic snapshot in `external_payment_commands` while frontend unknown-result copy stays stable; the generic error fallback for command messages is also sanitized before persistence. | Focused validation: `npm run check:baofu-withdrawal-workflow`; `npm run compile`; `npm run lint`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run 'TestCreateMerchantBaofuWithdrawal(RequiresIdempotencyKey\|ReplaysSameIdempotencyKey\|RejectsConflictingIdempotencyKey\|ManagerCanCreate)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run 'Test(BaofuWithdrawal\|CreateBaofuWithdrawal\|CreateMerchantBaofuWithdrawal\|ListBaofuWithdrawals\|GetBaofuWithdrawal\|BaofuWithdrawCallback)' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./logic -run 'TestBaofuWithdraw(Service\|Command)' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run 'Test(BaofuWithdrawalIdempotencyMigration261HardensAlreadyAppliedWeak260Schema\|CreateBaofuWithdrawalOrderIdempotencyUniquePerOwner\|CreateBaofuWithdrawalOrderRejectsPartialIdempotencyPair\|UpdateBaofuWithdrawalOrderStatusDoesNotRegressTerminal)$' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" go test ./worker -run 'Test(ProcessTaskBaofuWithdrawalFactApplication\|BaofuWithdrawalRecovery)' -count=1 -v`; `PATH="/usr/local/go/bin:$PATH" make check-generated`; `PATH="/usr/local/go/bin:$PATH" bash .github/scripts/test_backend_sql_guard.sh`; `PATH="/usr/local/go/bin:$PATH" make check-baofu-contract`; `git diff --check`. | Remaining withdrawal risk is now the pre-existing local `processing` row before provider create call: a process crash before the provider request can leave a row that recovery may not find provider-side unless the flow is redesigned around an outbox/command-send boundary. |
| 2026-06-10 | `merchant-manual-open-status` | this commit | G2 | Closed the 2026-06-10 manual-open precedence decision. Manual open/close now persists a durable `merchants.manual_open_status_until` boundary when `auto_open_by_business_hours` is enabled, calculated from database-local business-hours truth. The business-hours scheduler skips merchants with an active manual override until the next switching point, clears expired override markers when automatic truth resumes, and still fails closed immediately if payment configuration becomes invalid. API opening now also checks local `merchant_payment_configs` is active with a non-empty `sub_mch_id`, after Baofoo readiness succeeds. Same-day business-hour semantics are now explicit: non-closed reverse/cross-midnight rows are rejected by API validation and by a DB CHECK constraint, with historical dirty reverse/cross-midnight rows deleted before adding the constraint. | Focused validation: `make sqlc`; `go test ./api -run 'TestNextBusinessHoursSwitchAt|TestSetMerchantBusinessHours_(PersistsAutoOpenByBusinessHours|RejectsReverseOpenCloseWindow)|TestUpdateMerchantOpenStatus_(PublishesMerchantStatusChange|PublishesAutoCloseAt|RejectsOpenWhenPaymentConfigInactive|SetsManualOverrideUntilNextBusinessHoursSwitch|SetsInfiniteManualOverrideWhenAutoModeHasNoFutureSwitch)|TestGetMerchantOpenStatus_IncludesBaofuSettlementReadiness' -count=1`; `go test ./db/sqlc -run 'TestMerchantBusinessHoursSameDayWindowMigrationRemovesDirtyReverseRows|TestSyncMerchantOpenStatusByBusinessHours(PreservesManualOverrideUntilBoundary|ClosesWhenPaymentConfigInvalidDespiteManualOverride|SupportsMultipleSameDaySlots|SpecialDateRowsOverrideWeeklyRows)|TestClearExpiredMerchantManualOpenStatusOverrides|TestCreateBusinessHourRejectsNonClosedReverseWindow|TestAutoCloseMerchantsClosesExpiredManualOpenStatus' -count=1`; `go test ./scheduler -run TestMerchantOpenStatusScheduler -count=1`; `go test ./logic -run TestWantedMerchant -count=1`; `make check-generated`; `bash .github/scripts/test_backend_sql_guard.sh`; `git diff --check`. Broader `go test ./api ./db/sqlc ./scheduler ./logic -count=1` was also attempted: `scheduler` and `logic` passed, while `api` failed in existing Baofu settlement-account manager-permission mock expectations and `db/sqlc` failed in existing refund recovery coverage outside this merchant-status change. `make lint-filesize` still fails on the repository's pre-existing oversized-file baseline. | Manual override until the next business-hours switching point is closed. Remaining manual-status drift is low-risk UX/copy only: dashboard readiness precheck copy can still be improved for manager-facing clarity while backend readiness enforcement remains authoritative. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed representative high-level pending-staff route proof without changing production behavior. `GET /v1/kitchen/orders` and `POST /v1/inventory` now have real registered-route tests proving a pending staff association returns 403 at `MerchantStaffMiddleware` and does not reach downstream kitchen reads or inventory writes. This complements the existing SQL/helper/middleware/device-access coverage and narrows the remaining work to product decisions rather than an authorization proof gap. | Focused proof: `go test ./api -run 'Test(ListKitchenOrdersAPI\|CreateDailyInventoryAPI)$' -count=1 -v`; adjacent pending-staff auth proof: `go test ./api -run 'Test(MerchantStaffMiddleware\|ListAccessibleMerchantsExcludesPendingStaffRole\|GetMerchantDeviceAccessAPI)$' -count=1 -v`; final whitespace validation: `git diff --check`. | Representative high-level route proof is closed. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed Mini Program group-join duplicate-submit and re-entry recovery without changing the group approval transaction. Backend now exposes merchant-owner applicant history at `GET /v1/merchants/me/group-join-requests`, including `group_name` for display. Duplicate pending writes return stable API error code `40980`; the merchant join page branches on that code, not backend message text, then re-reads durable truth. The page reads durable history on entry/show, renders the latest pending request, reconciles search-result actions against pending state, blocks duplicate taps with page/row submitting state, and re-reads after create success or `40980` conflict. The old register join page now redirects to the single merchant-owner join page so there is not a second stale search/submit implementation. | Focused proof: `make sqlc`; `make swagger`; `go test ./db/sqlc -run 'Test.*GroupJoinRequest' -count=1 -v`; `go test ./api -run 'Test(ListMyGroupJoinRequestsAPI\|CreateGroupJoinRequestAPI\|ApproveGroupJoinRequestAPI\|CancelGroupJoinRequestAPI\|RejectGroupJoinRequestAPI)' -count=1 -v`; `make check-generated`; `bash .github/scripts/test_backend_sql_guard.sh`; Mini Program `npm run check:merchant-group-join-recovery`; `npm run compile`; `npm run check:wxml-handlers`; final `npm run quality:check`; `git diff --check`. | Mini Program duplicate-submit/re-entry recovery is closed for the merchant-owner applicant path. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed group join request pending-only uniqueness. Migration `000257` drops the old all-status `(group_id, merchant_id, status)` unique constraint, cleans duplicate historical pending rows by preserving the newest pending request and marking older pending rows cancelled, and adds a partial unique index on `(group_id, merchant_id) WHERE status='pending'`. This keeps active pending dedupe at the database boundary while allowing repeated rejected/cancelled history for audit and diagnosis. | Red-green/focused: `go test ./db/sqlc -run 'Test(CreateGroupJoinRequestTxRejectsDuplicatePendingRequest\|RejectGroupJoinRequestTxAllowsRepeatedRejectedHistory\|CancelGroupJoinRequestTxAllowsRepeatedCancelledHistory)' -count=1 -v`; broader join validation: `go test ./db/sqlc -run 'Test.*GroupJoinRequest' -count=1 -v`; `go test ./api -run 'Test(CreateGroupJoinRequestAPI\|ApproveGroupJoinRequestAPI\|CancelGroupJoinRequestAPI\|RejectGroupJoinRequestAPI)' -count=1 -v`; generation and guard validation: `make sqlc`; `make check-generated`; `bash .github/scripts/test_backend_sql_guard.sh`; `git diff --check`; isolated migration proof covered a clean DB through `000257` plus an old `256` DB with repeated rejected/cancelled history and duplicate pending rows upgraded through `000257`, including post-migration duplicate-pending rejection and extra rejected-history insert success. | Pending-only join uniqueness and terminal-history preservation are closed. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed group application active-draft uniqueness. Migration `000256` forward-cleans historical non-latest draft rows to `rejected`, adds a partial unique index on `(applicant_user_id) WHERE status='draft'`, and keeps dirty-schema upgrades convergent. `CreateGroupApplicationDraft` is now idempotent through the database upsert, latest application reads use `created_at DESC, id DESC`, and `ResetGroupApplicationToDraft` returns an existing draft for the applicant instead of forcing a second draft. | Red-green/focused: `go test ./db/sqlc -run TestCreateGroupApplicationDraftIsIdempotentForActiveDraft -count=1 -v` first failed with two different draft IDs, then passed after the upsert/constraint fix. `go test ./db/sqlc -run TestResetGroupApplicationToDraftReturnsExistingDraftWhenOneExists -count=1 -v` first failed with PostgreSQL `23505`, then passed after reset became idempotent. Validation: `make sqlc`; `go test ./db/sqlc -run 'TestCreateGroupApplicationDraftIsIdempotentForActiveDraft\|TestResetGroupApplicationToDraftReturnsExistingDraftWhenOneExists' -count=1 -v`; isolated migration proof for a clean DB through `000256` and an old `255` DB with duplicate drafts upgraded through `000256`, including post-migration unique-index rejection. | Active-draft uniqueness is closed at the DB/sqlc boundary. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed the pending-staff access leak and split staff/coarse-role propagation risk. Backend keeps the current pending model `merchant_staff(role='pending', status='active')` for pending workbench display, but role-agnostic authorization and default merchant resolution now exclude pending staff; `GetMerchantByOwner` also excludes pending staff before logic `resolveMerchantForUser` fallback can grant a merchant. Middleware still resolves pending associations only for explicit denial/audit. Direct add, invite bind, role assignment, and removal now call `AddMerchantStaffTx`, `AssignMerchantStaffRoleTx`, and `RemoveMerchantStaffTx`, so `merchant_staff` and coarse `user_roles.merchant_staff` create/reactivate/disable semantics happen in one transaction. `UpsertUserRoleActive` reactivates disabled coarse roles without swallowing unique-conflict errors inside an aborted transaction. | Red-green/focused: `go test ./api -run 'TestMerchantStaffMiddleware/OK_DefaultsToGrantedStaffMerchantWhenPendingAlsoExists' -count=1 -v` first failed with `400`, then passed after default selection preferred granted staff merchants over pending associations; `go test ./db/sqlc -run 'TestGetMerchantByOwner' -count=1 -v` failed on the pending-staff fallback before `GetMerchantByOwner` filtered `ms.role <> 'pending'`, then passed after regeneration. Validation: `make sqlc`; `make check-generated`; `go test ./db/sqlc -run 'TestGetMerchantByOwner\|Test.*MerchantStaff\|TestCheckUserHasMerchantAccess\|TestGetUserMerchantRole\|TestCountMerchantStaff\|TestUpsertUserRoleActive' -count=1`; `go test ./api -run 'Test.*MerchantStaff\|Test.*Staff\|TestGetMerchantDeviceAccessAPI\|TestListAccessibleMerchantsExcludesPendingStaffRole\|TestGetCurrentUserAPI_WithPendingMerchantWorkbench\|TestMerchantOwnerMiddleware\|TestMerchantOwnerOnlyMiddleware' -count=1`; `go test ./api -run 'Test(Table\|Reservation\|DiningSession\|MerchantStaffMiddleware\|GetMerchantDeviceAccessAPI\|ListAccessibleMerchantsExcludesPendingStaffRole\|BindMerchantDoesNotGrantMerchantStaffRoleWhenPending\|AddMerchantStaffAPIUsesAtomicStaffRoleTx\|UpdateMerchantStaffRoleAPIUsesAtomicStaffRoleTx\|DeleteMerchantStaffAPIUsesAtomicStaffRoleTx\|GetCurrentUserAPI_WithPendingMerchantWorkbench)' -count=1`; `go test ./logic -run 'TestResolveMerchantForUserRejectsPendingStaffFallback\|Test.*DiningSession\|Test.*Merchant\|Test.*Refund\|Test.*Reservation' -count=1`; `git diff --check`. | Pending staff role-aware authorization, staff/coarse-role atomicity, and disabled-staff reactivation are closed for the traced backend paths. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed group join create/reject/cancel split state/audit writes and the cancel terminal-state regression. `CreateGroupJoinRequestTx` now locks merchant affiliation before creating a pending request and audit log; `RejectGroupJoinRequestTx` and `CancelGroupJoinRequestTx` lock the request, require group/current-state preconditions, conditionally transition only pending rows, and write audit logs in the same transaction. The broad `UpdateGroupJoinRequestStatus` query was removed and replaced with pending-only `CancelPendingGroupJoinRequest`; stale cancel now maps to HTTP 409 instead of overwriting approved/rejected requests. The create precheck and in-transaction joined-merchant branches now both return stable `ErrMerchantAlreadyJoinedGroup`. | Red-green/focused: `go test ./api -run TestCreateGroupJoinRequestAPIConflictWhenPrecheckFindsJoinedMerchant -count=1 -v` failed first with generic `40900`, then passed with `40977`; `go test ./db/sqlc -run 'Test(CreateGroupJoinRequestTxWritesAuditAndRequestTogether\|CreateGroupJoinRequestTxRejectsJoinedMerchantInsideTransaction\|RejectGroupJoinRequestTxWritesAuditAndRejectsOnlyPending\|CancelGroupJoinRequestTxDoesNotOverwriteApprovedRequest)' -count=1 -v`; API proof: `go test ./api -run 'Test(CreateGroupJoinRequestAPI\|CreateGroupJoinRequestAPIConflictWhenTxFindsJoinedMerchant\|CreateGroupJoinRequestAPIConflictWhenPrecheckFindsJoinedMerchant\|RejectGroupJoinRequestAPIUsesAtomicTx\|CancelGroupJoinRequestAPI)' -count=1 -v`; generated/caller validation: `make sqlc`; `make swagger`; `make check-generated`; `go test ./db/sqlc -run 'Test.*GroupJoinRequest' -count=1 -v`; `go test ./api -run 'Test.*GroupJoinRequest.*API\|TestCreateGroupJoinRequestAPI' -count=1 -v`; `git diff --check`. | Group join state/audit atomicity, cancel race, and joined-merchant create conflict stability are closed. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed the group OCR JSON writeback race. `UpdateGroupApplicationLicense` now merges `application_data` JSONB patches instead of replacing the whole blob, and the group OCR API/worker pending, success, and failure writebacks pass only the current OCR document keys. Parallel business-license and ID-card OCR completions can no longer lose sibling document bindings/results through stale full-blob writes. | Red-green/focused: `go test ./db/sqlc -run TestUpdateGroupApplicationLicenseMergesApplicationDataPatch -count=1 -v` first failed on the replace-whole-blob path, then passed after SQL merge. Caller proof: `go test ./api -run 'TestCreateOCRJob_MarksGroup(BusinessLicense\|IDCard)Pending\|TestCreateOCRJob_GroupPendingRejectsInvalidExistingApplicationData\|TestMarkOCRFailed_GroupIDCardWritesPatchOnly' -count=1 -v`; `go test ./worker -run 'TestProcessTaskGroupApplication(BusinessLicense\|IDCard)OCR_(UsesOCRJob\|FailureWritesPatchOnly)' -count=1 -v`. Broader package validation: `go test ./api -count=1`; `go test ./worker -count=1`; `make sqlc`; focused db/sqlc group tests. | Group OCR JSON concurrency is closed. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Closed the group application direct license-asset trust-boundary gap. `PUT /v1/groups/applications/basic` now validates a submitted `license_image_asset_id` before writing the draft: the media asset must exist, belong to the applicant, have upload status `confirmed`, and use one of the same business-license categories accepted by the group OCR path (`business_license` or `group_license`). Cross-user assets are rejected with 403, wrong-category or unconfirmed uploads with 400, missing assets with 404, and media lookup infrastructure errors go through `internalError` without leaking raw details; rejected assets do not reach `UpdateGroupApplicationBasic`. | Red-green/focused: `go test ./api -run TestUpdateGroupApplicationBasicAPI -count=1 -v` first failed on the old path because no media lookup occurred and bad assets reached `UpdateGroupApplicationBasic`, then passed after the server-side validation. Adjacent OCR validation: `go test ./api -run 'Test(UpdateGroupApplicationBasicAPI\|CreateOCRJob_.*Group\|CreateOCRJob_DoesNotEnqueueWhenMarkPendingFails)' -count=1 -v`; `git diff --check`. | Direct group license-asset binding is closed for the applicant draft update path. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-06-09 | `merchant-staff-and-group` | this commit | G3 | Replaced the shared API duplicate-key classifier with typed PostgreSQL error-code classification. `isDuplicateKeyError` now uses `db.ErrorCode(err) == db.UniqueViolation`, so short ordinary errors can no longer panic through string slicing, non-unique driver errors are not misclassified by message text, and concurrent unique conflicts still map to the existing stable conflict/idempotent branches for staff role propagation, WeChat login user creation, web-login QR retries, group member creation, and group join request creation. | Red-green/focused: `go test ./api -run TestIsDuplicateKeyErrorUsesTypedPostgresCode -count=1 -v` first failed on the old short-error panic, then passed after the typed classifier. Caller-focused validation: `go test ./api -run 'Test(IsDuplicateKeyErrorUsesTypedPostgresCode\|EnsureMerchantStaffUserRoleActive\|BindMerchantDoesNotGrantMerchantStaffRoleWhenPending\|CreateGroup\|CreateGroupJoinRequest\|WechatLogin\|WebLogin)' -count=1 -v`; `git diff --check`. | Duplicate-key classification is closed. The invite-code follow-up is closed by the 2026-06-10 staff invite-code row above. |
| 2026-05-30 | `merchant-business-hours-auto-open` | `a0efffd3`, `9d47638e`, `7e9bd9d0` | G2 | Reworked the Mini Program business-hours time picker and sync action UI: time selection is usable, selected time is written back, the popup/body scroll conflict was fixed, first-slot values are preserved while adding another slot, and the sync action is placed as an independent right-side control. | WeApp build/debug loop during the business-hours session; commits `a0efffd3`, `9d47638e`, `7e9bd9d0`. | Backend scheduler multi-slot/special-date proof is added in the 2026-06-02 follow-up row. Special-date mixed closed/open semantics and legacy single-row helper queries remain audit follow-ups. |
| 2026-05-31 | `merchant-claim-recovery` | `024997f0` | G3 | Aligned merchant/rider claim recovery route and DTO contracts: Mini Program uses recovery-id `/v1/{role}/recoveries/:id` read/pay routes, merchant dispute APIs target `/v1/merchant/recovery-disputes/**`, list/detail expose `recovery_id` and `recovery_status`, and frontend status buckets use backend `disputed` with old `appealed` only as compatibility fallback. | Backend API/logic/worker/sqlc tests in the commit plus Mini Program `check-merchant-claim-recovery-contract.test.js`. Slice updated with fixed route/field notes. | Duplicate-dispute proof and full payment-fact-release convergence are added in the 2026-06-02 follow-up rows. Product still needs to decide whether a claim-id compatibility recovery route is required for external clients. |
| 2026-05-31 | `merchant-member-balance-adjust` | `62839932` | G3 | Hardened manual stored-value balance adjustment: backend now requires `Idempotency-Key`, adjustment transaction uses a durable idempotency key and conflict semantics, insufficient balance uses typed errors, member list pagination returns full total, Mini Program sends/reuses adjustment idempotency keys, transaction labels cover `adjustment_credit` and `adjustment_debit`, and order cancellation preserves principal/bonus split fields. | Backend membership API/logic/sqlc/order-status tests, generated docs/sqlc/mock, and Mini Program `check-merchant-member-balance-adjust-contract.test.js`. | Product decision 2026-06-10 manager permission and generated direct balance SQL writer retirement are closed by later rows. Optional future product/API work remains: whether manual adjustment responses should expose a transaction id as a durable audit anchor. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Aligned member-balance scene semantics to the confirmed product contract: only `dine_in` and `takeaway` are supported because stored-value funds are already held by the merchant and cannot fund split-settlement delivery/reservation order paths. Mini Program settings type/options now expose only `dine_in/takeaway`; migrations `000252/000253` forward-clean unsupported/null persisted scenes, align DB defaults/comments, and add scene CHECK constraints. `000253` exists because local validation found a database already marked at version `252` without the expected default/constraint convergence, so same-number migration edits would not replay there. | Red-green/focused: `node scripts/check-merchant-membership-settings-scenes.test.js`; backend, Mini Program, and migration validation are recorded in this commit. | Closed by later 2026-06-07 Mini Program `takeaway` checkout row: reachability and preview/direct consistency are now covered by `check:takeaway-balance-checkout`. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Closed membership settings partial-update drift. `PUT /v1/merchants/me/membership-settings` keeps optional-field semantics by loading the existing durable row as the merge base, falling back to defaults only when the row is missing, overlaying provided fields, preserving explicit empty scene arrays as disabled scene sets, and persisting the merged row through the existing upsert. | Focused validation for this commit covers logic/API regressions for omitted-field preservation, explicit empty arrays, missing-row default merge base, and adjacent membership payment/price tests; final commands are recorded with the commit. | Closed by later 2026-06-07 Mini Program `takeaway` checkout row: reachability and preview/direct consistency are now covered by `check:takeaway-balance-checkout`. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Added direct backend proof that member balance is usable for self-pickup orders. `POST /v1/orders` now has API coverage for `order_type=takeaway` with `use_balance=true`, proving the path validates membership/settings, creates a `takeaway` order without address/table/reservation fields, and forwards membership id plus balance-paid amount into `CreateOrderTx`. | `go test ./api -run 'TestCreateOrderWithBalanceAPI/WithBalance_Takeaway_OK' -count=1 -v`; broader membership order validation is recorded with this commit. | Closed by later 2026-06-07 Mini Program `takeaway` checkout row; backend direct-order support remains unchanged and the customer-facing path now sends `use_balance` when balance is selected. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Closed dine-in checkout balance-selector drift. The Mini Program dine-in checkout ViewState now disables stored-value payment from backend `payment_assessment.is_balance_payable` plus local member balance, and falls back to WeChat payment when a previously selected balance option becomes backend-disabled, so merchant settings or stacking rules are reflected before order creation instead of only failing at `POST /v1/orders`. | Red-green/focused: `node scripts/check-dine-in-checkout-balance-assessment.test.js` first failed on the old local-balance-only selector behavior, then passed after the ViewState fix. `npm run compile` was run for the Mini Program TypeScript surface. | Closed by later 2026-06-07 Mini Program `takeaway` checkout row; dine-in selector alignment remains covered by this row. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Added rules-engine parity proof for member-balance scenes. DB rules `balance_scene_allowed=true` now has focused coverage proving the rule condition follows the same contract as membership settings and direct payment validation: missing settings default to allowing `dine_in/takeaway`, explicit settings allow only configured supported scenes, and legacy `takeout/reservation` values do not make unsupported order types eligible. | Focused validation: `go test ./api -run 'TestDBRulesEngineEvaluate(BalanceSceneAllowedParity|_ReturnsErrorOnMalformedActiveRuleVersionJSON)' -count=1`. | Closed by later 2026-06-07 Mini Program `takeaway` checkout row; rules-engine scene parity remains proof-covered. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Added owner membership-settings API contract proof. `GET /v1/merchants/me/membership-settings` now has owner success coverage; `PUT /v1/merchants/me/membership-settings` has full-field owner success coverage and request-validation coverage for unsupported scenes plus out-of-range `max_deduction_percent`, proving invalid payloads return `400` before the logic upsert path. | Focused validation: `go test ./api -run 'Test(Get\|Update)MembershipSettingsAPI\|TestMerchantProfileWriteRoutesDenyLowPrivilegeStaff' -count=1`. | Closed by later 2026-06-07 Mini Program `takeaway` checkout row. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Added isolated migration fixture proof for membership scene hardening. The tests create scratch databases to prove a clean DB through `000253` gets the `dine_in/takeaway` defaults and CHECK constraints, dirty historical rows at version `251` are cleaned by `000252` before constraints are added, and a database already marked at weak `252` converges through `000253` without replaying the edited same-number migration. | Focused validation: `go test ./db/sqlc -run 'TestMembershipSettingsSceneMigration' -count=1 -v`. | Closed by later 2026-06-07 Mini Program `takeaway` checkout row. |
| 2026-06-07 | `merchant-membership-settings` | this commit | G2 | Closed customer-facing Mini Program `takeaway` stored-value checkout reachability and preview/direct consistency. Restaurant detail now lets customers choose `外卖代取` or `到店自取`, carries `order_type=takeaway` through dish/detail/combo/cart/order-confirm, calculates preview without address for self-pickup while preserving backend `payment_assessment`, exposes balance payment only when backend assessment and local balance allow it, omits address/delivery fields for `takeaway`, submits `use_balance` when selected, and treats balance-paid orders as terminal only from backend `status=paid` plus `payment_method=balance`. Adjacent takeout home feed and reorder paths now pin or forward the matching order type so stale cart-service state cannot move normal takeout items into the self-pickup cart. | Focused Mini Program validation: `npm run check:merchant-membership-settings-scenes`; `npm run check:dine-in-checkout-balance-assessment`; `npm run check:takeaway-balance-checkout`; `npm run compile`. Final full Mini Program validation: `npm run quality:check`. | `dine_in` and `takeaway` stored-value checkout proof is now covered; `takeout` delivery and `reservation` remain intentionally unsupported by product contract. |
| 2026-05-31 | `merchant-order-operations` | `6a19a9c0` | G3 | Fixed merchant manual refund creation from order detail: Mini Program refund wrapper now requires and sends `Idempotency-Key`, order detail creates a per-refund-draft key, reuses it for retry of the same unchanged draft, and refreshes it when the refund draft changes. | Mini Program `check-merchant-manual-refund-idempotency-contract.test.js`; payment/refund contract scripts updated. | Manual refund can now reach backend idempotency, but refund terminal convergence still depends on provider callback/query facts and the existing payment-domain outbox/recovery path. |
| 2026-05-31 | `merchant-order-operations` | `d3e84050` | G3 | Added recovery for existing normal-order refund rows stuck in `pending` after merchant reject or other order-refund paths: scheduler scans pending order refund rows for cancelled paid orders, dispatches refund tasks with the original `out_refund_no`, and refund worker validates reused refund rows against payment order and amount before calling the provider. | `go test ./worker -count=1`; focused `go test ./db/sqlc`; `make test-safety`; `make check-generated`; `make check-baofu-contract`; backend SQL guard and `git diff --check`. | UI/API visibility gap was closed by the 2026-06-01 repair. New retryable/queryable Baofu create failures are handled by the 2026-06-02 classification repair; historical terminal `failed` rows still require evidence-backed reconciliation rather than automatic retry by status. |
| 2026-06-01 | `merchant-order-operations` | `692ed4ef` | G3 | Added truthful merchant-reject refund submission state: backend returns `refund_submission.status/message/refund_id/out_refund_no`, preserves HTTP 200 when cancellation succeeds, distinguishes `accepted`, `pending_recovery`, `manual_required`, and `not_needed`, and keeps the reject response compatible with legacy top-level order fields. Mini Program and Flutter reject flows now display the backend refund message instead of hardcoded "已拒单并发起退款". | Focused logic/API/worker tests, `make swagger`, `make check-generated`, Mini Program lint, Flutter analyze, `make check-baofu-contract`, and broader safety validation in this branch. | New retryable/queryable Baofu create failures are handled by the 2026-06-02 classification repair. Web was only protected by the compatible top-level order response; no Web UI copy change was made in this Mini Program/App-focused pass. |
| 2026-06-02 | `merchant-order-operations` | this commit | G2 | Aligned merchant order realtime contract: backend declares `order_update`, order service publishes merchant snapshots through that event type, and Mini Program order list/detail plus kitchen board/detail subscribe to `order_update` and rehydrate from backend truth instead of waiting for notification-shaped messages. | `go test ./logic -run 'TestOrderService.*MerchantOrder\\|TestOrderServiceAcceptMerchantOrder\\|TestOrderServiceMarkMerchantOrderReady' -count=1`; `go test ./websocket -count=1`; Mini Program `check:merchant-order-update-websocket-contract`, `compile`, and `lint`. | Detail/list/kitchen realtime refresh now has a Mini Program contract test. Flutter push/poll/websocket auto-accept dedupe and BLE no-duplicate-print proof remain separate open items. Historical terminal refund rows remain a separate evidence-backed reconciliation question. |
| 2026-06-02 | `merchant-order-operations` | this commit | G3 | Classified Baofu refund create failures for new writes. Explicit non-retryable provider/business/config/validation failures still mark refund rows `failed` and command rows `rejected`; retryable create failures such as `SYSTEM_BUSY`, `REQUEST_FAILED`, and retryable `HTTP_STATUS` keep refund rows `pending` with command status `unknown`; queryable/possibly-created failures such as `ORDER_EXIST`, `REPEATED_REQUEST`, `BF0013`, `TRADE_UNCONFIRMED`, `PROCESSING`, and `ABNORMAL` move rows to `processing` for callback/query convergence. Pending manual-refund idempotency replay now resubmits the same `out_refund_no`, and the Baofu aggregate refund client preserves `FAIL.errCode` before success-response field validation so the classifier sees provider truth. | Focused logic, worker, and Baofu aggregate client tests cover retryable provider errors, `ORDER_EXIST` query recovery, pending idempotency replay, merchant-reject submission semantics, and worker recovery behavior. Final branch validation is recorded in this commit. | Historical terminal `failed` refund rows are still not auto-retried. A future migration or reconciliation job should only touch them if durable command/fact evidence proves the original failure class was retryable/queryable; otherwise leave them for manual platform reconciliation. Manual refund `pending` rows are replayable with the same idempotency key, but broad automatic recovery remains modeled for cancelled-order/refund-recovery paths rather than arbitrary manual refund attempts. |
| 2026-06-02 | `merchant-business-hours-auto-open` | this commit | G2 | Added DB-level scheduler proof for automatic business-hours status transitions: multiple same-day slots now have regression coverage for opening inside a matching slot and closing outside all slots, and special-date rows now have regression coverage proving they override weekly rows in both close-over-weekly-open and open-over-weekly-closed cases. Existing scheduler SQL already satisfied the contract; this branch adds durable proof rather than changing production SQL. | `go test ./db/sqlc -run 'Test.*BusinessHour\|TestSyncMerchantOpenStatusByBusinessHours' -count=1`; `git diff --check`. | Mixed special-date rows that include any `is_closed=true` row still close the whole day by current `BOOL_OR(is_closed)` semantics; product should confirm whether that is intended holiday semantics before changing it. `GetBusinessHourByDayOfWeek` and `GetBusinessHourByDate` remain legacy single-row helper candidates and should not be used for multi-slot editing/status decisions. |
| 2026-06-02 | `merchant-claim-recovery` | this commit | G3 | Hardened duplicate recovery-dispute submission semantics. The existing DB unique index on `(claim_id, appellant_type)` remains the durable duplicate guard; logic now maps concurrent unique-conflict windows to stable business semantics instead of leaking PostgreSQL 23505 as an internal error. Merchant duplicate create maps to the existing conflict response; same-rider duplicate create reloads the existing dispute and returns `AlreadyExists`. | `go test ./logic -run 'TestCreate.*RecoveryDispute' -count=1`; `go test ./db/sqlc -run 'TestCreateRecoveryDisputeWithRecoveryTx_DuplicateDoesNotWriteSecondEvent\|TestCreateRecoveryDisputeWithRecoveryTx_WritesDisputedRecoveryEvent' -count=1`; `go test ./api -run 'TestCreateMerchantRecoveryDisputeAPI\|TestCreateRiderRecoveryDisputeAPI' -count=1`. | Payment-fact-release convergence is closed in the next 2026-06-02 row. Product should still decide whether a claim-id compatibility recovery route is required for external clients; manager dispute-create permission has registered-route proof in the 2026-06-10 row. |
| 2026-06-07 | `merchant-manual-open-status` | this commit | G2 | Closed the `auto_close_at` runtime gap. The existing `MerchantOpenStatusScheduler` now calls `AutoCloseMerchants` before business-hours sync, reads back each changed merchant status, and publishes `merchant_status_change` with source `auto_close`, so timed manual opening can converge without adding another scheduler or API contract. `AutoCloseMerchants` remains the durable SQL boundary for closing only open merchants whose manual `auto_close_at` has expired and clearing the field after close. | Red-green/focused: `go test ./scheduler -run TestMerchantOpenStatusScheduler -count=1`; persistence proof: `go test ./db/sqlc -run 'TestAutoCloseMerchantsClosesExpiredManualOpenStatus\|TestSyncMerchantOpenStatusByBusinessHours' -count=1`. | Manual open/close versus `auto_open_by_business_hours=true` now follows the 2026-06-10 product decision: manual status should override until the next business-hours switching point; current scheduler order can still let automatic business-hours state overwrite manual changes too soon. Cross-client Mini Program/App convergence proof remains a separate test gap. |
| 2026-06-08 | `merchant-manual-open-status` | this commit | G2 | Added cross-client open-status convergence proof without changing production behavior. `check:merchant-open-status-cross-client-contract` now locks the backend `GET/PATCH /v1/merchants/me/status` route and `is_open` contract, the Mini Program dashboard service/switch/readback path, the Flutter `WorkingStatusNotifier.syncFromBackend/setStatus` read/write path, Flutter order-list status actions, and Flutter login sync against the same backend truth. The check is included in Mini Program `quality:check`. | Red-green/focused: missing npm script failed first; then `npm run check:merchant-open-status-cross-client-contract` passed. Adjacent Flutter proof: `flutter test test/working_status_provider_test.dart test/working_status_sync_manager_test.dart`. | Manual open/close versus `auto_open_by_business_hours=true` now follows the 2026-06-10 product decision: manual status should override until the next business-hours switching point; current scheduler order can still let automatic business-hours state overwrite manual changes too soon. Dashboard failure-state UI proof is closed by the 2026-06-09 row below. |
| 2026-06-09 | `merchant-manual-open-status` | this commit | G2 | Closed Mini Program dashboard open-status failure-state UI proof. The dashboard now renders the existing `refreshErrorMessage` as a TDesign warning notice with retry when refresh or partial status sync fails, while the switch still changes local `isOpen` only from the backend PATCH response, keeps duplicate-submit loading visible, and clears submitting state in `finally`. The new `check:merchant-open-status-dashboard-failure-state` contract is included in Mini Program `quality:check`. | Red-green/focused: `npm run check:merchant-open-status-dashboard-failure-state` failed first because `refreshErrorMessage` was never rendered, then passed after the WXML/WXSS fix. Adjacent validation: `npm run check:merchant-open-status-cross-client-contract`; `npm run compile`; `npm run lint -- --quiet`. | Manual open/close versus `auto_open_by_business_hours=true` now follows the 2026-06-10 product decision: manual status should override until the next business-hours switching point; current scheduler order can still let automatic business-hours state overwrite manual changes too soon. Manager-facing readiness precheck copy is still optional because backend readiness enforcement remains authoritative. |
| 2026-06-08 | `merchant-app-bind-and-device` | this commit | G3 | Closed the App bind tenant-boundary gap for public bind-code verification. `verifyAppBindCode` now requires the Redis-embedded `merchantID` to match one of the user's current active merchant roles before minting long-lived App tokens, so a user moved to another merchant cannot exchange an old code. Added focused proof for generate -> verify -> session creation -> one-time reuse denial with Redis cleanup, plus a changed-merchant-role rejection regression. | Red-green/focused: `go test ./api -run TestVerifyAppBindCodeRejectsChangedMerchantRole -count=1 -v` first failed on the old path because it continued to `GetUser`; after the fix, `go test ./api -run 'Test(VerifyAppBindCodeRejectsChangedMerchantRole\|AppBindCodeGenerateVerifyCreatesSessionAndPreventsReuse)' -count=1 -v` passed. | The code-consumption-order, Flutter platform-payload, active logout unregister, and backend stale-device cleanup follow-ups were closed on 2026-06-11. Remaining App bind/device follow-up: permanent push failure handling now has a product decision to record reason/count, mark only the affected registered device/push token degraded first, and deactivate only that device/token after repeated clear terminal failures or long heartbeat absence. |
| 2026-06-08 | `merchant-reservation-and-table` | this commit | G3 | Reconciled a stale audit finding for merchant reservation `date+status` list totals. Existing code already computes the date-filter branch `total` from filtered rows for `date+status` and `date+exception`, so status tabs no longer report the raw date-only count. This pass updates the audit row, next-step item, and slice to match implementation truth; no production code change was required. | Re-verified implementation with `git blame` on `locallife/api/table_reservation.go` and focused API coverage: `go test ./api -run 'TestListMerchantReservations(DateScopedFilters\|ExceptionRequiresDate\|API)' -count=1 -v`. | Backend total semantics are closed. Mini Program action-permission and frontend date+status total-consumption contract proof remain separate test gaps; other reservation/table risks remain open. |
| 2026-06-08 | `merchant-reservation-and-table` | this commit | G3 | Closed table create/update tag partial-write risk. `POST /v1/tables` and `PATCH /v1/tables/:id` now prevalidate submitted `tag_ids` as positive, non-duplicate, existing `table` tags before mutation; tagged create/update calls `CreateTableTx` / `UpdateTableTx`, so table metadata and tag associations commit or roll back together. | Red-green/focused: sqlc transaction tests first failed because `CreateTableTx`/`UpdateTableTx` did not exist, then passed after implementation. Focused validation: `go test ./api -run 'Test(CreateTableAPI\|CreateTableAPIRejectsInvalidTagIDsBeforeCreate\|UpdateTableAPI\|UpdateTableAPIRejectsNonTableTagBeforeMutating)' -count=1 -v`; `go test ./db/sqlc -run 'Test(CreateTableTxRollsBackWhenTagInsertFails\|UpdateTableTxRollsBackWhenTagInsertFails\|CreateTable\|UpdateTable\|AddTableTag\|RemoveAllTableTags\|ListTableTags)' -count=1 -v`. | Table tag replacement is closed for create/update. Later rows close table image media validation, end-to-end reservation-to-dining-session proof, Mini Program status-copy proof, Flutter table media-category alignment, Mini Program action/total proof, and Mini Program partial table-image recovery. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-08 | `merchant-reservation-and-table` | this commit | G3 | Closed table image media trust-boundary validation and table-image action binding. `POST /v1/tables/:id/images` now loads the submitted media asset and rejects non-`table` categories, unconfirmed uploads, private media, unapproved media, cross-merchant uploaders, and pending staff uploaders before clearing a primary image or inserting `table_images`; owner uploads and active merchant-staff uploads remain accepted. `PUT /v1/tables/:id/images/:image_id/primary` now switches primary image through a transaction that first requires the image to belong to the path table, so missing/foreign images return 404 without clearing the current primary. `DELETE /v1/tables/:id/images/:image_id` now deletes by `(table_id, image_id)` and returns 404 for missing/foreign images instead of deleting across tables. | Red-green/focused: `go test ./api -run TestAddTableImageAPI -count=1` first failed on the old handler because bad media still reached `SetPrimaryTableImage`/`AddTableImage`; set-primary/delete review regressions first failed on the old handler because set-primary cleared before image lookup and delete mapped missing images to 500. After implementation: `go test ./api -run 'Test(AddTableImageAPI\|ListTableImagesAPI\|DeleteTableImageAPI\|SetTableImagePrimaryAPI\|GetRoomDetailAPI_WithImages)' -count=1`; `go test ./db/sqlc -run 'Test(SetTableImagePrimary\|SetTableImagePrimaryTx\|DeleteTableImage\|AddTableImage\|ListTableImages\|GetPrimaryTableImage)' -count=1`. | Backend table-image binding is closed for add, set-primary, and delete. Later rows close end-to-end reservation-to-dining-session proof, Mini Program status-copy proof, Flutter table media-category alignment, Mini Program action/total proof, and Mini Program partial table-image recovery. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-08 | `merchant-reservation-and-table` | this commit | G3 | Closed table future-reservation update/disable/delete drift and the adjacent reservation-create race. `DELETE /v1/tables/:id` now uses `DeleteTableTx` to lock the table row first and returns stable API outcomes for locked missing rows (404), active `current_reservation_id` (409), and future `pending|paid|confirmed` reservations (409). `PATCH /v1/tables/:id` now routes requests touching table number/type/capacity/minimum-spend or disabled status through `UpdateTableTx`; `PATCH /v1/tables/:id/status` routes disabled requests through `UpdateTableStatusTx`; both transactions lock the table row, map locked missing rows to 404 after the initial ownership precheck, and only reject actual reservation-critical changes when future reservations exist. Customer and merchant reservation creation now route through `CreateReservationTx` / `CreateMerchantReservationTx`, wait on the same table row lock, and revalidate the locked merchant/type/status/capacity truth before insert; customer create also reapplies locked minimum-spend pricing. Table-number QR cleanup is also protected inside the transaction. | Red-green/focused: API tests first failed because no-op critical-field and already-disabled requests bypassed the transaction path; merchant-create API first failed because it still expected direct `CreateTableReservationByMerchant`; sqlc tests first failed because guarded no-op critical-field updates and already-disabled requests were rejected, table-number changes in `UpdateTableTx` did not clear existing QR, reservation create did not reject a disabled table after lock, delete lock-after-missing initially mapped to 500 instead of 404, and update/disable lock-after-missing initially mapped to 500 instead of 404. After implementation: `go test ./api -run 'Test(MerchantCreateReservationAPI(Success\|RejectsDisabledTableAfterLock\|RejectsDeletedTableAfterLock)\|CreateReservationAPI\|UpdateTableAPI\|UpdateTableAPIRejects(FulfillmentChangeWithFutureReservations\|DeletedTableAfterPrecheck)\|UpdateTableStatusAPI\|UpdateTableStatusAPIRejects(DisableWithFutureReservations\|DeletedTableAfterPrecheck)\|DeleteTableAPI)' -count=1 -v`; `go test ./db/sqlc -run 'Test(DeleteTableTxRejects(MissingTable\|ActiveReservationAfterLock)\|Create(ReservationTxRejectsDisabledTableAfterLock\|MerchantReservationTxRejectsDisabledTableAfterLock)\|UpdateTable(TxRejectsFutureReservationsWhenGuarded\|TxAllowsNoopFulfillmentFieldsWithFutureReservationsWhenGuarded\|TxClearsQRCodeWhenTableNoChanges\|StatusTxRejectsFutureReservationsWhenGuarded\|StatusTxAllowsAlreadyDisabledTableWithFutureReservationsWhenGuarded))' -count=1 -v`. | Backend table delete/update/disable and reservation-create locked-table revalidation are closed for the API and transaction paths. Later rows close end-to-end reservation-to-dining-session proof, Mini Program status-copy proof, Flutter table media-category alignment, Mini Program action/total proof, and Mini Program partial table-image recovery. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-08 | `merchant-reservation-and-table` | this commit | G3 | Closed the merchant reservation-edit transaction gap left after locked reservation creation. `PUT /v1/reservations/:id/update` now calls `UpdateReservationTx` instead of doing ownership/terminal/conflict checks outside the write boundary. The transaction locks the reservation row, verifies merchant ownership and non-terminal status, locks the target table row when table/date/time/guest count changes, revalidates merchant/type/status/capacity and online minimum-spend constraints, checks final same-table date/time conflicts inside the transaction, and maps locked missing/disabled target-table conflicts to stable API responses. | Red-green/focused: sqlc edit tests prove disabled target table, over-capacity guest count, and time conflict all reject without mutating the reservation: `go test ./db/sqlc -run 'TestUpdateReservationTxRejects(DisabledTargetTableAfterLock\|GuestCountExceedsLockedCapacity\|TimeConflictAfterLock)' -count=1 -v`. API tests prove the update endpoint uses `UpdateReservationTx` and maps locked disabled/missing target tables to stable 409/404 responses: `go test ./api -run 'TestMerchantUpdateReservationAPI' -count=1 -v`. `make mock` regenerated the Store mock for `UpdateReservationTx`. | Backend merchant reservation edit is now aligned with locked table truth. Later rows close end-to-end reservation-to-dining-session proof, Mini Program status-copy proof, Flutter table media-category alignment, Mini Program action/total proof, and Mini Program partial table-image recovery. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-09 | `merchant-reservation-and-table` | this commit | G3 | Closed the end-to-end reservation-to-dining-session proof gap without changing production behavior. `TestMerchantReservationEditOpenAndCloseDiningSessionFlow` now proves merchant-created reservation create/edit reaches the sqlc dining-session transaction boundary, marks the reservation `checked_in`, occupies the edited target table, closes the dining session, closes the billing group, completes the reservation, and releases the table. `TestUpdateTableStatusAPI/ManualReleaseClosesActiveDiningSession` locks the Mini Program table-release API path so requesting `available` on a table with an active dining session calls `CloseDiningSessionTx` and returns the released table instead of bypassing session closure. | Focused validation: `go test ./db/sqlc -run TestMerchantReservationEditOpenAndCloseDiningSessionFlow -count=1 -v`; `go test ./api -run 'TestUpdateTableStatusAPI/ManualReleaseClosesActiveDiningSession' -count=1 -v`. Broader package and generated checks are recorded with this commit. | Backend reservation-to-dining-session convergence proof is closed. Mini Program status-copy proof is closed by the next 2026-06-09 row; Mini Program action/total proof and partial table-image recovery are closed by later 2026-06-09 rows. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-09 | `merchant-reservation-and-table` | this commit | G3 | Closed the Mini Program reservation/table status-copy contract gap. Reservation confirm dialog now states that confirmation only moves the reservation to `已确认` and does not occupy the table; actual occupancy comes from opening the dining session/开台. Reservation completion copy scopes table release to cases where the current table still links to the reservation. Manual table release copy now discloses that releasing can close the current dining session and complete the linked reservation before the table becomes available. The new contract check is included in `quality:check`. | Red-green/focused: `node scripts/check-merchant-reservation-table-status-contract.test.js` first failed on the old confirm copy, then passed after copy alignment. Focused validation: `npm run check:merchant-reservation-table-status-contract`; package JSON parse check; final Mini Program validation is recorded with this commit. | Status-copy drift is closed. Mini Program action/total proof and partial table-image recovery are closed by later 2026-06-09 rows. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-09 | `merchant-reservation-and-table` | this commit | G3 | Closed Mini Program/Flutter table-image media-category drift. Flutter table config-sheet and image-gallery upload entrypoints now send `mediaCategory: 'table'` to `MerchantImagePicker`, matching backend `validateTableImageMediaAsset` enforcement of `media.CategoryTableImage`; `table_image_media_category_contract_test.dart` reads the backend table handler and both Flutter upload entrypoints so stale `table_cover` cannot reappear unnoticed. | Red-green/focused: `flutter test test/table_image_media_category_contract_test.dart` first failed on the old `table_cover` category, then passed after the fix. Final validation: `flutter analyze`; `flutter test`; `git diff --check`. | Media-category alignment is closed. Mini Program action/total proof is closed by the 2026-06-09 row below; partial table-image recovery is closed by a later 2026-06-09 row. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-09 | `merchant-reservation-and-table` | this commit | G3 | Closed the Mini Program reservation action-permission and date+status total-consumption proof gap without changing production behavior. `check:merchant-reservation-actions-contract` now locks backend `MerchantActionState` exposure, Mini Program DTO support for `merchant_action_state`, frontend preference for backend `primary_action_key`, primary/more-action rendering from view-state flags, selected `date/status` list params, and `response.total` use for pagination and summary copy. | Red-green/focused: missing npm script failed first; then `npm run check:merchant-reservation-actions-contract` passed after adding the contract check and wiring it into `quality:check`. Final validation is recorded with this commit. | Action-permission and total-consumption proof is closed. Partial table-image recovery is closed by the next 2026-06-09 row. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-09 | `merchant-reservation-and-table` | this commit | G3 | Closed Mini Program new-table image-binding partial recovery. When table creation succeeds but one or more pending image bindings fail, the table edit page now remains open as the created table's edit page, keeps uploaded `mediaId` values pending instead of pretending they are persisted images, renders a persistent warning, refreshes the previous table list, and retries pending bindings on the next save; successful retry clears the recovery state and then navigates back. | Red-green/focused: `node scripts/check-merchant-table-image-bind-recovery.test.js` first failed because the old flow navigated back after post-create binding failure. After implementation: `npm run check:merchant-table-image-bind-recovery`; `npm run compile`; `npm run lint`. | Mini Program partial table-image recovery is closed. The offline/phone identity follow-up is closed by the 2026-06-10 row above; Flutter table flow/websocket proof is closed by the 2026-06-09 row below. |
| 2026-06-09 | `merchant-reservation-and-table` | this commit | G3 | Closed the Flutter table flow/websocket proof gap without changing production behavior. `table_flow_websocket_contract_test.dart` locks Flutter `TableRepository` usage of `/tables/**` list/detail/create/patch/delete/status/image/QR endpoints, `TableNotifier` load/create/update/status/delete state convergence, duplicate status-action single-flight behavior, and websocket table-status patching of only the matching row. `ws_client_test.dart` now proves backend `table_status_change` messages route to the table callback. | Focused validation: `flutter test test/table_flow_websocket_contract_test.dart`; `flutter test test/table_flow_websocket_contract_test.dart test/ws_client_test.dart`. Final validation is recorded with this commit. | Flutter table flow/websocket proof is closed. The offline/phone identity follow-up is closed by the 2026-06-10 row above. |
| 2026-06-08 | `merchant-marketing-rules` | this commit | G2 | Closed discount-rule partial-update validation drift. `CreateDiscountRule` and `UpdateDiscountRuleForMerchant` now share the same value validator; update reads the existing rule, merges omitted fields with incoming patch values, rejects invalid effective date range, non-positive amounts, and `discount_amount >= min_order_amount`, and returns a request 400 before `UpdateDiscountRule` can write. | Red-green/focused: `go test ./logic -run 'Test(CreateDiscountRuleValidation\|UpdateDiscountRuleForMerchantRejectsInvalidEffectiveValues)' -count=1 -v`; API boundary proof: `go test ./api -run TestUpdateDiscountRuleAPIRejectsInvalidEffectiveValues -count=1 -v`; adjacent regression: `go test ./logic -run 'Test(Create\|Update\|Delete\|Get\|List\|Applicable\|Best).*DiscountRule' -count=1`. | Later 2026-06-08 rows close delivery-promotion merged validation, discount static-route proof, voucher quantity mapping, and voucher/discount pagination truth; later 2026-06-09 rows close cart/order preview/direct-create marketing-total parity and delivery-promotion DB direct-writer constraints. Voucher template disable/delete semantics are closed by the 2026-06-10 row. |
| 2026-06-08 | `merchant-marketing-rules` | this commit | G2 | Closed delivery-promotion partial-update validation drift at the API boundary. `createDeliveryPromotion` and `updateDeliveryPromotion` now share the same value validator; update loads the existing promotion, merges omitted fields with incoming patch values, validates the effective time range and `discount_amount <= min_order_amount`, and rejects invalid partial updates with 400 before `UpdateDeliveryPromotion` can write. Optional update fields now carry the same name/amount binding constraints as create. | Red-green/focused: `go test ./api -run TestUpdateDeliveryPromotionAPIRejectsInvalidEffectiveValues -count=1 -v` failed on the old handler because it called `UpdateDeliveryPromotion`; after the fix, `go test ./api -run 'TestUpdateDeliveryPromotionAPI(RejectsInvalidEffectiveValues\|AcceptsValidEffectiveValues)' -count=1 -v` and `go test ./api -run 'Test(Create\|List\|Delete)DeliveryPromotionAPI\|TestUpdateDeliveryPromotionAPI(RejectsInvalidEffectiveValues\|AcceptsValidEffectiveValues)' -count=1` passed. | Later 2026-06-08 rows close discount static-route proof, voucher quantity mapping, and voucher/discount pagination truth; later 2026-06-09 rows close cart/order preview/direct-create marketing-total parity and delivery-promotion DB direct-writer constraints. Voucher template disable/delete semantics are closed by the 2026-06-10 row. |
| 2026-06-08 | `merchant-marketing-rules` | this commit | G2 | Closed discount static-route reachability proof without changing production routes. `TestDiscountStaticRoutesReachable` sends real requests to `/v1/merchants/:id/discounts/applicable` and `/best`, asserts both bypass `GetDiscountRule`, and proves they call `GetApplicableDiscountRules` / `GetBestDiscountRule` even though `GET("/:id")` is registered earlier. | Focused proof: `go test ./api -run TestDiscountStaticRoutesReachable -count=1 -v`; adjacent API regression: `go test ./api -run 'Test(GetApplicableDiscountRulesAPI\|GetBestDiscountRuleAPI\|DiscountStaticRoutesReachable)' -count=1`. | Later 2026-06-08 rows close voucher quantity mapping and voucher/discount pagination truth; later 2026-06-09 rows close cart/order preview/direct-create marketing-total parity and delivery-promotion DB direct-writer constraints. Voucher template disable/delete semantics are closed by the 2026-06-10 row. |
| 2026-06-08 | `merchant-marketing-rules` | this commit | G2 | Closed voucher quantity-update product error mapping. `PATCH /v1/merchants/:id/vouchers/:voucher_id` now uses the already loaded voucher row to reject `total_quantity < claimed_quantity` with 400 before `UpdateVoucher` can hit the database check constraint, so merchants receive a stable product-level validation response and the DB constraint remains a direct-writer safety net. | Red-green/focused: `go test ./api -run 'TestUpdateVoucherAPI/BadRequest_TotalQuantityBelowClaimed' -count=1 -v` first failed on the old handler because it called `UpdateVoucher`; after the fix it passed. Adjacent regression: `go test ./api -run 'Test(UpdateVoucherAPI\|CreateVoucherAPI\|DeleteVoucherAPI\|ClaimVoucherAPI)' -count=1`. | Later 2026-06-08 row closes voucher/discount pagination truth; later 2026-06-09 rows close cart/order preview/direct-create marketing-total parity and delivery-promotion DB direct-writer constraints. Voucher template disable/delete semantics are closed by the 2026-06-10 row. |
| 2026-06-08 | `merchant-marketing-rules` | this commit | G2 | Closed voucher/discount management list pagination truth. `GET /v1/merchants/:id/vouchers` now calls `CountMerchantVouchers`, and `GET /v1/merchants/:id/discounts` now calls an authorized `CountMerchantDiscountRules` logic path, so response `total` is the full matched backend count instead of the current page length. | Red-green/focused: `go test ./api -run 'TestListMerchantVouchersAPI/OK_ReturnsFullMatchedTotal' -count=1 -v` and `go test ./api -run 'TestListMerchantDiscountRulesAPI/OK_ReturnsFullMatchedTotal' -count=1 -v` first failed with `actual: 2`; after the fix they passed. Additional validation: `go test ./api -run 'TestListMerchantVouchersAPI\|TestListMerchantDiscountRulesAPI\|Test(GetApplicableDiscountRulesAPI\|GetBestDiscountRuleAPI\|DiscountStaticRoutesReachable)' -count=1`; `go test ./logic -run 'Test(ListMerchantDiscountRulesForbidden\|CountMerchantDiscountRulesForbidden\|Create\|Update\|Delete\|Get\|List\|Applicable\|Best).*DiscountRule' -count=1`. | Later 2026-06-09 rows close cart/order preview/direct-create marketing-total parity and delivery-promotion DB direct-writer constraints. Voucher template disable/delete semantics are closed by the 2026-06-10 row. Mini Program list probe logic is now a compatibility fallback and can be cleaned up separately. |
| 2026-06-09 | `merchant-marketing-rules` | this commit | G2 | Closed broader marketing total parity and fixed order-preview cart selection drift. `CalculateOrderPreview` now passes `order_type` into `GetCartByUserAndMerchant`, matching cart preview's typed cart lookup instead of querying with an empty order type. `TestOrderServiceCreateOrder_MarketingTotalsMatchCartAndOrderPreview` exercises takeout cart preview, order preview, and direct `OrderService.CreateOrder` with the same merchant discount, issued voucher, delivery fee, and delivery-fee discount, then asserts subtotal, merchant discount, voucher amount, delivery fee, delivery-fee discount, and final total converge before persistence. | Red-green/focused: `go test ./logic -run TestOrderServiceCreateOrder_MarketingTotalsMatchCartAndOrderPreview -count=1 -v` first failed because `CalculateOrderPreview` called `GetCartByUserAndMerchant` with empty `OrderType`; after the fix it passed. Adjacent regression: `go test ./logic -run 'Test(OrderServiceCreateOrder\|CalculateOrderPreview\|CalculateCartPreview\|PromotionEngine\|CalculateFinalPrice)' -count=1`. | Voucher template disable/delete semantics are closed by the 2026-06-10 row. Mini Program voucher/discount list probe cleanup remains optional compatibility cleanup. |
| 2026-06-09 | `merchant-marketing-rules` | this commit | G2 | Closed delivery-promotion DB direct-writer protection. Migration `000254` forward-cleans historical invalid `merchant_delivery_promotions` rows, then adds CHECK constraints for positive amounts, `discount_amount <= min_order_amount`, and `valid_until > valid_from`. The API binding/value validator now rejects `min_order_amount <= 0` and equal date bounds before store writes, preserving stable 400 responses instead of leaking DB constraint failures to clients. | Red-green/focused: the new DB direct-write test failed against a temporary schema migrated only through `000253` because invalid rows inserted successfully, then passed after `000254`. Validation: `go test ./db/sqlc -run 'TestDeliveryPromotion(RejectsInvalidDirectWrites|ConstraintMigration)' -count=1 -v`; `go test ./api -run 'Test(CreateDeliveryPromotionAPI\|UpdateDeliveryPromotionAPIRejectsInvalidEffectiveValues\|UpdateDeliveryPromotionAPIRejectsNonPositiveMinOrderAmountBeforeStoreUpdate\|UpdateDeliveryPromotionAPIAcceptsValidEffectiveValues)' -count=1 -v`; clean scratch migration to version `254` and dirty `253 -> 254` migration fixture proof in `TestDeliveryPromotionConstraintMigration*`. | Voucher template disable/delete semantics are closed by the later 2026-06-10 row, and recharge-rule deactivate/delete provenance is closed by the later 2026-06-10 row. Broader save-to-consumer proof for recharge bonus resolution and voucher rollback remains optional. |
| 2026-06-10 | `merchant-marketing-rules` | this commit | G2 | Closed the product-approved voucher-template disable/delete usability contract. User available voucher lists now filter joined voucher templates by `deleted_at IS NULL`, `is_active=true`, and current template validity; `GetUserVoucher` still returns issued voucher rows but carries a `voucher_template_block_reason` for deleted, inactive, not-started, or expired templates; preview/direct voucher validation and promotion-engine voucher trials reject blocked templates with stable customer copy; `CreateOrderTx` and `UseVoucherTx` now lock and recheck the voucher template inside the transaction so a template disabled, soft-deleted, not-yet-started, or expired between preview and order creation cannot be used. Existing orders and cancellation rollback stay snapshot-based and are not revalidated against current template status. | Red-green/focused: `go test ./db/sqlc -run '^Test(CreateOrderTxRejectsUnavailableVoucherTemplate\|UseVoucherTxRejectsUnavailableTemplate\|GetUserVoucherReturnsTemplateBlockReason)$' -count=1 -v` first failed on the old transaction path because inactive templates could still be used, then passed after adding transactional template recheck. Validation: `make sqlc`; `go test ./db/sqlc -run '^Test(ListUserAvailableVouchers\|ListUserAvailableVouchersFiltersInactiveAndDeletedTemplates\|ListUserAvailableVouchersForMerchant\|ListUserAvailableVouchersForMerchantFiltersInactiveAndDeletedTemplates\|GetUserVoucherReturnsTemplateBlockReason\|CreateOrderTxRejectsUnavailableVoucherTemplate\|UseVoucherTxRejectsUnavailableTemplate)$' -count=1 -v`; `go test ./logic -run '^(TestOrderServiceCreateOrderMapsVoucherTemplateUnavailable\|TestOrderServiceCreateOrder_RejectsVoucherWhenMerchantDiscountCannotStack\|TestOrderServiceCreateOrder_MarketingTotalsMatchCartAndOrderPreview\|TestValidateVoucher\|TestIsVoucherValid\|TestSuggestBestVoucher)$' -count=1 -v`. | Voucher template disable/delete/new-validity blocking is closed for list, preview, direct order creation, and transaction race paths. Recharge-rule deactivate/delete provenance is closed by the later 2026-06-10 row. Optional broader save-to-consumer proof for recharge bonus resolution and voucher rollback plus Mini Program voucher/discount list probe cleanup remain compatibility/coverage follow-ups. |
| 2026-06-10 | `merchant-marketing-rules` | this commit | G2 | Closed recharge-rule delete provenance drift. `DeleteRechargeRule` now updates `recharge_rules.is_active=false` and `updated_at=NOW()` instead of physically deleting the row, so deleted rules disappear from active/matching consumer reads while historical `membership_transactions.recharge_rule_id` still points at the exact rule used by past recharges. | Red-green/focused: `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run '^TestDeleteRechargeRule$' -count=1 -v` first failed because the old delete removed the rule and `GetRechargeRule` returned no rows, then passed after changing delete to deactivate. Validation: `PATH="/usr/local/go/bin:$PATH" go test ./db/sqlc -run 'Test(.*RechargeRule|.*MembershipTransaction|.*MembershipRecharge)' -count=1`; `PATH="/usr/local/go/bin:$PATH" go test ./logic -run 'Test(.*RechargeRule|.*MembershipRecharge)' -count=1`; `PATH="/usr/local/go/bin:$PATH" go test ./api -run 'Test(DeleteRechargeRuleAPI|CreateRechargeRuleAPI|BuildRechargeRuleStatusResponse|.*MembershipRecharge)' -count=1`; `PATH="/usr/local/go/bin:$PATH" make sqlc`; `PATH="/usr/local/go/bin:$PATH" make check-generated`; `PATH="/usr/local/go/bin:$PATH" bash .github/scripts/test_backend_sql_guard.sh`; `git diff --check`. | Recharge-rule delete provenance is closed. Optional remaining marketing work is broader save-to-consumer proof for recharge bonus resolution and voucher rollback, plus Mini Program voucher/discount list probe cleanup as compatibility cleanup. |
| 2026-06-09 | `merchant-device-display-config` | this commit | G2 | Closed the cloud-printer physical-delete/history-log drift. Migration `000255` adds `cloud_printers.deleted_at` and replaces the global printer-SN uniqueness with an active-row partial unique index. `DeleteCloudPrinter` now soft-deletes/deactivates rows instead of physically deleting them, current printer reads/lists/updates exclude soft-deleted rows, historical `print_logs` keep their printer reference, the same SN can be registered again after deletion, Yilianyun authorization refresh/rebind ignores stale soft-deleted printer links before attaching a replacement, historical print-job status can read soft-deleted printers without issuing provider queries, soft-deleted retry attempts are recorded as `printer is deleted`, and migration down tombstones duplicate soft-deleted SNs before restoring the old global unique index. | Red-green/focused: `go test ./db/sqlc -run TestDeleteCloudPrinterSoftDeletesWithHistoricalPrintLogs -count=1 -v` first failed on `print_logs_printer_id_fkey`, then passed after soft delete; new compile-red tests forced `GetCloudPrinterIncludingDeleted` and mock propagation before implementation; focused validations include `go test ./db/sqlc -run 'Test(GetCloudPrinterIncludingDeletedKeepsHistoricalPrinterReadable\|CloudPrinterSoftDeleteMigrationDownHandlesReusedSerialNumber\|DeleteCloudPrinterSoftDeletesWithHistoricalPrintLogs)' -count=1 -v`, `go test ./api -run 'Test(GetMerchantOrderPrintJobStatusAPI\|RetryMerchantOrderPrintJobAPI)' -count=1`, and `go test ./worker -run 'TestProcessTaskPrintOrder_Retry(SoftDeletedPrinterRecordsFailedAttempt\|PrintLogReplaysOriginalContent)' -count=1 -v`. Adjacent validation: `go test ./db/sqlc -run 'Test(DeleteCloudPrinterSoftDeletesWithHistoricalPrintLogs\|CreateAuthorizedYilianyunCloudPrinterTx\|UpsertCloudPrinterProviderAuthorization\|GetCloudPrinterProviderAuthorization)' -count=1 -v`; `go test ./api -run 'Test(Create\|List\|Get\|Update\|Delete\|Test)PrinterAPI\|TestGetPrinterLiveStatusAPI\|TestYilianyun' -count=1`; `go test ./worker -run 'Test.*Print\|TestPaymentDomainOutbox.*AutoAccept\|TestCloudPrinterStatus\|TestYilianyun' -count=1`. | Later 2026-06-10 rows closed Mini Program voice-control removal, backend `voice_*` no-op compatibility, `enable_print`/`auto_accept_paid_orders` normalization, and Flutter App backend-truth alert auto-accept convergence. Remaining device/display follow-ups are merchant-facing reconciliation job UI or wrapper cleanup and Flutter BLE backend-config/observability/dedupe implementation. |
| 2026-06-02 | `merchant-claim-recovery` | this commit | G3 | Closed claim-recovery payment release convergence: claim recovery fact application now surfaces the release action created by `ProcessPaymentSuccessTx`, the payment fact application worker immediately enqueues it on the critical behavior-action queue, and durable `created/failed` release actions still have scheduler recovery. Integration helpers now exercise pay request -> WeChat direct payment fact application -> release action execution -> suspension clear instead of directly calling the payment-success transaction. | `go test ./integration -run 'TestClaimJourneyD(10\|12)\w*RecoveryPayIntegration' -count=1`; `go test ./logic -run 'TestPaymentFactServiceApplyExternalPaymentFactApplication_ClaimRecovery\|TestCreate.*ClaimRecoveryPayment\|TestCreate.*RecoveryDispute' -count=1`; `go test ./worker -run 'ClaimRecovery\|RecoveryDispute\|PaymentFactApplication' -count=1`; `go test ./db/sqlc -run 'TestProcessPaymentSuccessTx_ClaimRecovery\|TestCreateRecoveryDisputeWithRecoveryTx\|TestReviewRecoveryDispute\|TestMarkClaimRecoveryOverdue' -count=1`; `go test ./api -run 'Test.*RecoveryDispute\|Test.*ClaimRecovery\|TestHandlePaymentNotify.*ClaimRecovery' -count=1`; `git diff --check`. | Paid/waived release remains an async side effect, so if enqueue/worker execution is delayed the release action is recoverable but merchant-facing delayed-release visibility is still a product gap. Claim-id compatibility remains a product decision; manager dispute-create permission has registered-route proof in the 2026-06-10 row. |
| 2026-06-02 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed the direct inventory POST tenant-boundary gap. `POST /v1/inventory` now loads the requested dish before insert, returns 404 when the dish is missing, returns 403 when the dish belongs to another merchant, and only creates the daily inventory row after merchant ownership is proven. Swagger annotations were aligned with the existing 201 success and new 403 branch. | `go test ./api -run TestCreateDailyInventoryAPI -count=1`; broader inventory/API/generated validation is recorded in this commit. | Other inventory and dish workflow issues remain open: `is_available` reader/editor semantics, dish-edit full-workflow partial failure recovery, and unused wrapper cleanup. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed featured-tag error swallowing and per-call partial persistence. `PUT /v1/dishes/:id/featured-tags` now filters allowed featured names in the handler, delegates replacement to `SetDishFeaturedTagsTx`, preserves non-featured dish tags, propagates store/list/upsert failures, and rolls back the featured-tag replacement on failure. | `go test ./api -run TestSetDishFeaturedTagsAPI -count=1`; `go test ./db/sqlc -run 'TestSetDishFeaturedTagsTx' -count=1`; broader validation is recorded in this commit. | The larger Mini Program dish edit workflow is still multi-call: base dish save can commit before featured-tags or customizations fail. `is_available` semantics and unused wrapper cleanup remain open. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed the finite inventory total invariant. Merchant `PUT /v1/inventory` and `PATCH /v1/inventory/:dish_id` now reject finite totals below sold plus reserved quantities before create/update, and migration `000244` repairs historical contradictory rows then adds a DB check constraint so direct store writes cannot reintroduce the state. | `go test ./api -run 'Test(CreateDailyInventoryAPI|UpdateDailyInventoryAPI|UpdateSingleInventoryAPI|CheckAndDecrementInventoryAPI|GetInventoryStatsAPI)' -count=1`; `go test ./db/sqlc -run 'TestUpdateDailyInventory|TestReserveInventory|TestCheckAndDecrementInventory' -count=1`. | Remaining inventory/dish issues: `is_available` reader/editor semantics, dish-edit full-workflow partial failure recovery, and unused wrapper cleanup. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Aligned inventory stats with reserved-stock truth. `GetInventoryStats` now counts finite rows as sold out when `sold_quantity + reserved_quantity >= total_quantity` and only counts them available when uncommitted stock remains, matching list/save availability and order/reservation reserve guards. | Red-green: `go test ./db/sqlc -run TestGetInventoryStatsTreatsReservedQuantityAsCommitted -count=1`; validation: `go test ./db/sqlc -run 'TestGetInventoryStats|TestReserveInventory|TestCheckAndDecrementInventory' -count=1`; `go test ./api -run TestGetInventoryStatsAPI -count=1`. | Remaining inventory/dish issues: `is_available` reader/editor semantics, dish-edit full-workflow partial failure recovery, and unused wrapper cleanup. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed batch status partial-update false success. `BatchUpdateDishOnlineStatus` now returns actual SQL-updated IDs, and `PATCH /v1/dishes/batch/status` reports only those IDs in `updated`, moving stale/deleted prefiltered misses into `failed` instead of claiming all valid-looking IDs succeeded. | Red-green: `go test ./api -run TestBatchUpdateDishStatusAPIReportsOnlyActualUpdatedRows -count=1`; `go test ./db/sqlc -run TestBatchUpdateDishOnlineStatusReturnsActualUpdatedIDs -count=1`. Validation: `go test ./api -run 'Test(UpdateDishStatusAPI\|BatchUpdateDishStatusAPIReportsOnlyActualUpdatedRows\|SetDishFeaturedTagsAPI)' -count=1`; `go test ./db/sqlc -run 'Test(UpdateDishOnlineStatus\|BatchUpdateDishOnlineStatusReturnsActualUpdatedIDs)' -count=1`; `make check-generated`; `git diff --check`. | Remaining inventory/dish issues: `is_available` reader/editor semantics, dish-edit full-workflow partial failure recovery, and unused wrapper cleanup. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed inventory check contract drift. `POST /v1/inventory/check` now verifies dish ownership, reads `GetDailyInventory` without mutating `sold_quantity`, treats missing inventory rows and `total_quantity=-1` as unlimited stock, and returns insufficient inventory based on `total - sold - reserved`. Mini Program `checkInventory` wrapper signatures now use backend `CheckInventoryRequest` and `CheckInventoryResponse`. | Red-green: `go test ./api -run TestCheckInventoryAPI -count=1`. Validation: `go test ./api -run 'Test(CreateDailyInventoryAPI\|UpdateDailyInventoryAPI\|UpdateSingleInventoryAPI\|CheckInventoryAPI\|GetInventoryStatsAPI)' -count=1`; `go test ./db/sqlc -run 'Test(CheckAndDecrementInventory\|ReserveInventory\|GetInventoryStats)' -count=1`; Mini Program changed-file ESLint; `npm run compile`; `make check-generated`; `git diff --check`. | Remaining inventory/dish issues: `is_available` reader/editor semantics, dish-edit full-workflow partial failure recovery, and unused wrapper cleanup. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed inventory stats Mini Program contract drift. All seven `DishManagementService.getInventoryStats` copies now send backend query shape `{ date }` and return `InventoryStatsResponse`, matching `/v1/inventory/stats` swagger fields `total_dishes`, `unlimited_dishes`, `sold_out_dishes`, and `available_dishes`. Added a contract check that scans backend handler, swagger, and all wrapper copies. | Red-green: `node scripts/check-inventory-stats-contract.test.js`. Broader Mini Program validation is recorded in this commit. | Remaining inventory/dish issues: `is_available` reader/editor semantics, dish-edit full-workflow partial failure recovery, and unused wrapper cleanup. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed dish `is_available` semantic drift by choosing it as the real merchant-controlled temporary availability flag. Mini Program dish edit now loads/submits backend `is_available`, renders a `是否可售` switch, and still forces packaging dishes available. Public dish detail, merchant/global search, recommendation ID/detail readers, scan-table menu, and reservation item validation now exclude or reject unavailable dishes consistently with existing order/cart behavior. | Red-green: `node scripts/check-merchant-dish-availability-contract.test.js`; `go test ./logic -run TestValidateReservationItemsRejectsUnavailableDish -count=1`; `go test ./api -run TestGetPublicDishDetailRejectsUnavailableDish -count=1`; `go test ./db/sqlc -run 'Test(SearchDishesByNameExcludesUnavailable\|SearchDishesGlobalExcludesUnavailable\|GetDishesByIDsFiltersUnavailable\|GetDishesWithMerchantByIDsFilterUnavailable\|ListDishesForMenuFiltersUnavailable)' -count=1`. Broader validation is recorded in this commit. | Remaining inventory/dish issues: dish-edit full-workflow partial failure recovery and unused wrapper cleanup. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed the Mini Program dish-edit partial-save recovery gap. After a base dish create/update succeeds, later featured-tag or customization failures now keep the page in edit mode with the saved dish id, set an explicit partial-save recovery state, render a persistent TDesign warning, and let the same save action retry the pending sync. | Red-green: `node scripts/check-merchant-dish-edit-partial-save-recovery.test.js`; adjacent contract: `node scripts/check-merchant-dish-availability-contract.test.js`; broader Mini Program validation is recorded in this commit. | The edit workflow is still three backend writes; a future single backend workflow transaction could simplify it. Current remaining inventory/dish cleanup is unused wrapper retirement. |
| 2026-06-03 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed unused Mini Program dish/inventory wrapper exposure. The copied dish API files no longer export unused `batchUpdateDishStatus`, `checkInventory`, or `getInventoryStats` wrappers, and the stale inventory-stats wrapper contract check was replaced with an unused-wrapper cleanup guard. | Red-green: `node scripts/check-merchant-dish-unused-wrapper-cleanup.test.js`; adjacent contracts: `node scripts/check-merchant-dish-availability-contract.test.js`, `node scripts/check-merchant-dish-edit-partial-save-recovery.test.js`; Mini Program compile and changed-file ESLint are recorded in this commit. | Backend batch/status/check/stats routes remain available and tested. Future page binding should add a fresh task-owned wrapper and contract test instead of reintroducing stale copied surface. |
| 2026-06-09 | `merchant-dish-status-and-inventory` | this commit | G2 | Closed the backend zombie SQL cleanup for dish availability. The legacy `UpdateDishAvailability` query had no runtime caller after `is_available` became part of the supported dish update contract, so it was removed from `db/query`, generated sqlc, `Querier`, and `MockStore`; remaining tests now set availability through `UpdateDish`. | Focused validation: `go test ./db/sqlc -run 'Test(UpdateDishCanSetAvailability\|PublicComboQueriesExcludeUnavailableChildDishes)' -count=1`; regeneration validation: `make sqlc`; final generated check is recorded with this commit. | Dish/inventory known repair items are closed. Future single-backend-workflow transaction for the three-step dish edit remains an optional product/architecture simplification, not an active correctness blocker. |
| 2026-06-06 | `merchant-combo-and-catalog` | this branch | G2 | Closed category sort-only false success. `PATCH /v1/dishes/categories/:id` now persists sort-only updates through `UpdateMerchantDishCategoryOrder` after merchant-category ownership is verified, and the response uses the persisted `merchant_dish_categories.sort_order` value instead of only echoing the request. | Red-green: `go test ./api -run TestUpdateDishCategorySortOrderAPI -count=1`; validation: `go test ./api -run 'Test(UpdateDishCategorySortOrderAPI|CreateDishCategoryAPI|ListDishCategoriesAPI)' -count=1`; `go test ./api -run 'Test.*DishCategory.*API' -count=1`; `go test ./db/sqlc -run TestUpdateMerchantDishCategoryOrder -count=1`. | Later combo/catalog rows below close merchant combo tag creation permission and direct combo-dish endpoint retirement. |
| 2026-06-06 | `merchant-combo-and-catalog` | this branch | G2 | Closed combo child-dish availability drift across write, public-read, and customer-action paths. `POST /v1/combos`, `PUT /v1/combos/:id`, `PUT /v1/combos/:id/online`, then-active direct `POST /v1/combos/:id/dishes`, public combo detail/list/scan/search/recommendation SQL, cart add/update, direct order calculation, and reservation item validation now fail closed when combo children are empty, missing, soft-deleted, offline, or unavailable. Existing-child dependency failures map to logged 500s while business precondition failures remain stable 4xx responses. | Red-green/focused coverage at the time: `go test ./logic -run 'Test(AddCartItem_RejectsUnavailableComboChildDish\|UpdateCartItem_RejectsUnavailableComboChildDish\|CalculateOrderItems/ComboChildUnavailable\|CalculateOrderItems/SuccessCombo\|ValidateReservationItemsRejectsUnavailableComboChildDish)' -count=1`; `go test ./api -run 'TestGetPublicComboDetailAPI_(ApprovedMerchantKeepsOpenState\|RejectsUnavailableChildDish)\|Test(UpdateComboSet\|AddComboDish\|ToggleComboOnline)API' -count=1`; `go test ./db/sqlc -run 'TestPublicComboQueriesExcludeUnavailableChildDishes\|TestGetCombosWithMerchantByIDs\|TestSearchCombosGlobal_ExcludesTakeoutSuspendedMerchants' -count=1`. Broader validation is recorded in that commit; the direct add route is retired by a later row below. | Later combo/catalog rows below close merchant combo tag creation permission and direct combo-dish endpoint retirement. |
| 2026-06-06 | `merchant-combo-and-catalog` | `9f7da49a` | G1 | Closed combo delete association/comment drift by making the API/Swagger contract match the actual persistence semantics. `DELETE /v1/combos/:id` is documented as a soft delete that hides `combo_sets` while retaining `combo_dishes` and `combo_tags` rows for historical/audit use, and sqlc coverage now proves those association rows remain after delete. | `go test ./db/sqlc -run TestDeleteComboSet -count=1`; `go test ./db/sqlc -run 'TestDeleteComboSet|TestGetComboSet|TestListComboDishes|TestAddComboTag' -count=1`; `go test ./api -run TestDeleteComboSetAPI -count=1`; `make check-generated`; `git diff --check`. | Later combo/catalog rows below close merchant combo tag creation permission and direct combo-dish endpoint retirement. |
| 2026-06-06 | `merchant-combo-and-catalog` | this commit | G2 | Closed category hidden-reference drift across delete and rename. `DELETE /v1/dishes/categories/:id` now calls `UnlinkUnusedMerchantDishCategoryTx`, locks the merchant-category link, and conditionally unlinks only when no active dish references the category; active references return 409. Dish create/update transactions lock target category links before writing `dishes.category_id`, and `RenameMerchantDishCategoryTx` locks the old category link before migrating/unlinking it so concurrent writers fail closed after the rename commits. | Red-green: `go test ./db/sqlc -run 'TestUnlinkUnusedMerchantDishCategoryTx|TestCreateDishTxRejectsUnlinkedCategory|TestUpdateDishTxRejectsUnlinkedCategory|TestRenameMerchantDishCategoryTxSerializesOldCategory(Create|Update)Writers' -count=1`; `go test ./api -run 'Test(DeleteDishCategoryRejectsActiveDishesAPI|CreateDishAPI/CategoryUnlinkedDuringCreate|UpdateDishAPI/CategoryUnlinkedDuringUpdate)' -count=1`; final generated and broader validation are recorded in this commit. | Later combo/catalog rows below close merchant combo tag creation permission and direct combo-dish endpoint retirement. |
| 2026-06-06 | `merchant-combo-and-catalog` | this commit | G2 | Closed merchant combo tag permission drift by making the Mini Program combo edit page selection-only for combo tags. The page still loads backend-approved `combo` tags, but no longer renders the unsupported `新增标签` action, no longer opens a creation dialog, and no longer calls admin-only `TagService.createTag({ type: 'combo' })`. | Red-green: `node scripts/check-merchant-combo-tag-permission-contract.test.js`; validation: `npm run check:merchant-combo-tag-permission-contract`; `npm run compile`; `npm run check:wxml-handlers`; `git diff --check -- <changed combo tag files>`. | Later row below closes direct combo-dish endpoint retirement. |
| 2026-06-06 | `merchant-combo-and-catalog` | this commit | G2 | Closed direct combo-dish endpoint drift by retiring the narrow legacy add/remove write path. Backend no longer registers `POST /v1/combos/:id/dishes` or `DELETE /v1/combos/:id/dishes/:dish_id`, Casbin and Swagger no longer expose them, and Mini Program copied dish API files no longer export `AddComboDishBodyRequest`, `addDishToCombo`, or `removeDishFromCombo`. Full combo create/update with `dishes[]` remains the supported child-association write contract; lower-level SQLC add/remove helpers remain available to combo transactions. | Red-green/focused: `go test ./api -run TestDirectComboDishRoutesRetiredAPI -count=1`; `node scripts/check-merchant-combo-direct-dish-wrapper-cleanup.test.js`; regeneration/generated: `make swagger`, `make check-generated`; final validation: targeted combo API tests, Mini Program compile, changed API ESLint, wrapper cleanup check, search guards, `jq` codegraph check, and `git diff --check`. | No current Mini Program merchant workflow depends on direct combo-dish add/remove. The removed HTTP path returns 404 instead of entering authz/business logic, so clients must use `POST /v1/combos` or `PUT /v1/combos/:id` with `dishes[]`. |
| 2026-06-06 | `merchant-review-reply` | this commit | G2 | Closed merchant review page owner-access drift. Mini Program now gates the review-management page through `ensureMerchantReviewManagementAccess`, allows only `merchant`/`merchant_owner` roles, preserves generic merchant-denied copy for non-merchant users, and renders owner-only denied copy for non-owner merchant staff before all-review/reply APIs are called. Backend review-management routes remain owner-only. | Red-green/focused: `npm run check:merchant-review-owner-access`; final validation for this row is recorded in this commit. | Remaining review-reply issues are separate: optional reply history/clear/versioning/idempotent-replay product decisions and missing OpenID/content-safety provider failure copy. |
| 2026-06-06 | `merchant-review-reply` | this commit | G2 | Added durable hidden-review reply visibility proof without changing production behavior. API/sqlc tests now prove merchant owner all-review/reply paths include hidden reviews, public merchant review list remains `is_visible=true` only after a hidden review is replied to, and the customer/user list still returns the user's hidden review. | Focused validation: `go test ./api -run TestReviewHiddenVisibilityAndReplyContractAPI -count=1`; `go test ./db/sqlc -run TestHiddenReviewReplyVisibilityContract -count=1`. | Reply history/idempotency and OpenID/content-safety provider failure copy also remain open. |
| 2026-06-06 | `merchant-review-reply` | this commit | G2 | Added durable reply overwrite/timestamp proof without changing production behavior. `UpdateMerchantReply` remains a single-field overwrite contract: repeated identical replies are treated as new edits and refresh `replied_at`, later different replies overwrite the stored reply, and review visibility is unchanged. | Focused validation: `go test ./db/sqlc -run TestUpdateMerchantReplyOverwriteAndTimestampContract -count=1`. | Product can still separately decide whether to add reply history, clear/delete support, optimistic versioning, or idempotent replay semantics. OpenID/content-safety provider failure copy remains open. |
| 2026-06-06 | `merchant-review-reply` | this commit | G2 | Added durable merchant all-review image resolver proof without changing production behavior. The owner-only all-review endpoint returns hidden reviews but image enrichment follows the public resolver: approved public review images are returned, pending review images are withheld from merchants, and media asset ids are not exposed. | Focused validation: `go test ./api -run TestListMerchantAllReviewsAPI_UsesPublicReviewImageResolver -count=1`. | Product can separately request an owner/merchant-visible resolver if merchants should see pending customer review images. OpenID/content-safety provider failure copy remains open. |
| 2026-06-06 | `merchant-review-reply` | this commit | G2 | Closed Mini Program reply failure copy for the traced content-safety identity/provider branches. Reply submit failures now route through `getMerchantReviewReplyErrorMessage`, mapping missing WeChat OpenID, risky text, and content-safety provider/gateway failures to Chinese product copy without leaking backend English, OpenID terminology, or WeChat provider diagnostics. | Red-green/focused: `node scripts/check-merchant-review-reply-error-copy.test.js`; final validation is recorded in this commit. | Product can still separately decide whether to add reply history, clear/delete support, optimistic versioning, idempotent replay semantics, or merchant-visible pending review images. |
| 2026-06-06 | `merchant-profile-update` | this commit | G2 | Closed merchant category replacement partial-write risk. `PUT /v1/merchants/me/tags` now rejects duplicate tag IDs, validates merchant tags before write, and delegates replace-all to `SetMerchantTagsTx`, which locks the merchant row then clears, inserts, and lists tags inside `execTx` so same-merchant replacements serialize and insertion failures roll back the old category set. | Red-green/focused: `go test ./db/sqlc -run TestSetMerchantTagsTxRollsBackWhenReplacementFails -count=1`; `go test ./db/sqlc -run TestSetMerchantTagsTxRejectsMissingMerchant -count=1`; `go test ./api -run TestSetMerchantTagsAPIUsesTransactionalReplacement -count=1`; duplicate-input branch: `go test ./api -run TestSetMerchantTagsAPIRejectsDuplicateTagIDs -count=1`. Final validation is recorded in this commit. | Category replacement is closed. Shop-image scoping is closed in the next ledger row; frontend pending-sync recovery and logo conflict rollback/user guidance remain open. |
| 2026-06-06 | `merchant-profile-update` | this commit | G2 | Closed shop-image application-row scoping, staff-owner targeting, and search/category reader duplication/stale-cover risk. `PATCH /v1/merchants/me/shop-images` now fails closed without middleware merchant context, uses the resolved merchant owner user id for the application-image write, `UpdateMerchantApplicationShopImages` selects one latest editable application row with `ORDER BY created_at DESC, id DESC LIMIT 1`, and shop-image JSON serialization errors are handled explicitly before persistence. `SearchMerchants` and `SearchMerchantsByTag` now join only one latest editable application image row per merchant owner. | Red-green/focused: `go test ./api -run 'TestUpdateCurrentMerchantShopImages(FailsClosedWhenMerchantContextMissing|UsesResolvedMerchantOwnerForStaff|_ReturnsInternalServerErrorOnInvalidStoredStorefrontImages)' -count=1`; `go test ./db/sqlc -run 'TestGetMerchantApplicationDraftUsesIDTieBreaker|TestUpdateMerchantApplicationShopImagesTargetsLatestApplicationOnly|TestSearchMerchantsUsesLatestApplicationImagesOnly|TestSearchMerchantsByTagUsesLatestApplicationImagesOnly' -count=1`. Final validation is recorded in this commit. | Live storefront/environment images still live on `merchant_applications` keyed by owner user, not a merchant-owned image table/field. Product/schema work can later move live image truth if merchant-specific application ownership is required. Frontend pending-sync recovery proof and logo conflict rollback/user guidance remain open. |
| 2026-06-07 | `merchant-profile-update` | this commit | G2 | Closed Mini Program Logo PATCH conflict recovery after media upload. The profile-images page now routes Logo persistence failures through a dedicated mapper, treats HTTP 409 and 409xx domain codes as version conflicts, restores the previous trusted Logo state after failed persistence, shows conflict-specific retry copy, and forces a latest profile reload so the optimistic version and backend Logo truth are refreshed. | Red-green/focused: `npm run check:merchant-profile-images-logo-conflict-recovery`; validation also includes Mini Program compile/lint and final review for this commit. | The uploaded media asset may remain unreferenced after a PATCH conflict/failure; this is existing media lifecycle behavior. Remaining `merchant-profile-update` follow-ups are frontend pending-sync recovery proof for storefront/environment images and the product/schema decision whether live shop images should move off `merchant_applications`. |
| 2026-06-07 | `merchant-profile-update` | this commit | G2 | Closed storefront/environment pending-sync recovery proof without changing production logic. The Mini Program check now exercises the helper/runtime recovery contract for ambiguous shop-image persistence: unconfirmed server echo keeps the pending local image visible and schedules retry, confirmed echo clears pending markers and retry state, reload resumes retry for local asset ids absent from server truth, and missing public URLs resume media polling. | Red-green/focused: `npm run check:merchant-profile-images-pending-sync-recovery`; validation also includes Mini Program compile/lint/quality gate and final review for this commit. | Live storefront/environment images still live on `merchant_applications`; product/schema work can later move live image truth if merchant-specific application ownership is required. |
| 2026-06-07 | `merchant-profile-update` / `merchant-application-onboarding` | `e5fb36fb` | G2/G3 | Closed approved-merchant live shop-image truth migration and owner-only approval lookup. Migration `000249` adds `merchants.storefront_images/environment_images` with JSON string-array/count constraints, rejects non-array and non-string-array merchant writes safely, and backfills valid image arrays from the latest approved application only for owners with one non-deleted merchant. `PATCH /v1/merchants/me/shop-images` writes the resolved merchant row, approval copies valid application image arrays into merchant live truth and drops invalid legacy application image JSON, `GET /v1/merchants/me` exposes public-resolved live arrays including explicit empty arrays, public/search readers prefer merchant live images with approved-only single-store fallback, Mini Program profile-images reads merchant live truth before application fallback, and `ApproveMerchantApplicationTx` now uses owner-only `GetMerchantOwnedByUser` plus application-owner matching so staff-associated or mismatched-owner merchants are not updated by applicant approval. | Red-green/focused: `go test ./db/sqlc -run 'Test(SearchMerchantsUsesMerchantShopImages\|SearchMerchantsFallsBackOnlyToApprovedApplicationImages\|SearchMerchantsDoesNotFallbackApplicationImagesForMultiStoreOwner\|GetLatestApprovedMerchantApplicationByUserDoesNotFallbackForMultiStoreOwner\|SearchMerchantsByTagUsesMerchantShopImages\|SearchMerchantsByTagFallsBackOnlyToApprovedApplicationImages\|SearchMerchantsByTagDoesNotFallbackApplicationImagesForMultiStoreOwner\|UpdateMerchantShopImagesTargetsMerchantOnly\|UpdateMerchantShopImagesRejectsNonStringImageArrays\|ApproveMerchantApplicationTxCopiesApplicationImagesToMerchantTruth\|ApproveMerchantApplicationTxIgnoresStaffAssociatedMerchant\|ApproveMerchantApplicationTxRejectsMismatchedApplicationOwner)' -count=1`; `go test ./api -run 'Test(GetCurrentMerchantReturnsResolvedLiveShopImageURLs\|GetPublicMerchantDetail_RewritesCoverImageInLocalMode\|GetPublicMerchantDetailFallsBackToApprovedApplicationImagesWhenMerchantImagesAreNull\|GetPublicMerchantDetailDoesNotFallbackToDraftApplicationImages\|GetPublicMerchantDetailDoesNotFallbackWhenMerchantImagesAreEmpty\|GetPublicMerchantDetail_ReturnsInternalServerErrorOnInvalidCoverImageJSON\|UpdateCurrentMerchantShopImages)' -count=1`; `go test ./logic -run 'Test(BuildMerchantApprovalTxParams_IncludesImagePayloads\|BuildMerchantApprovalTxParams_DropsInvalidLegacyImagePayloads\|MerchantOnboardingReviewServiceProcessSubmittedApplication_ActivatesCredentialsAndAttemptsRestore)' -count=1`; `npm run check:merchant-profile-images-pending-sync-recovery`; `make sqlc`; `make check-generated`. Final package/generated/migration validation is recorded in this commit. | Application image fields remain for onboarding/draft and temporary compatibility fallback when merchant live fields are `NULL`. Multi-store owners with ambiguous historical application ownership intentionally do not use application fallback until live truth is written by the merchant endpoint or a future merchant-specific reconciliation. A future cleanup can remove fallback only after production rows are confirmed migrated and old Mini Program versions are no longer relevant. |
| 2026-06-07 | `merchant-profile-update` | this commit | G2 | Hardened already-applied live shop-image constraints. The root cause of `TestUpdateMerchantShopImagesRejectsNonStringImageArrays` failing in existing test databases was that an earlier weaker `000249` had already been applied, so `migrate.Up()` would not replay the corrected same-number migration file and the live `merchants` constraints still allowed non-string JSON array elements. Migration `000250` forward-fixes those environments by replacing the storefront/environment image constraints, cleaning invalid live JSON to `NULL`, and re-adding the string-array/count checks without touching onboarding application image history. | Focused validation: `go test ./db/sqlc -run TestUpdateMerchantShopImagesRejectsNonStringImageArrays -count=1 -v` advanced the existing test DB to migration `250` and proved the non-string-array write is rejected. Migration validation also covers a clean scratch DB running all migrations through `250`, a simulated old-`249` weak-constraint scratch DB containing non-string arrays before applying `000250`, and a dirty-schema scratch DB containing scalar, over-limit, valid, and `NULL` live image values before applying `000250`. | `000250` intentionally clears invalid live merchant image JSON to `NULL`, so those rows may use approved single-store application fallback until a merchant-specific image write or future reconciliation restores clean live truth. Do not edit an already-applied migration number to fix deployed schema drift; use forward migrations. |
| 2026-06-07 | `merchant-finance-withdrawal` | `5ea8634c` | G3 | Preserved Baofu account-opening/provider failure reasons while keeping sensitive-data boundaries. Provider diagnostics and account-opening snapshots keep provider error codes plus sanitized upstream reason fields for internal investigation; persisted failure messages retain the provider reason after sanitization, and settlement-account responses can surface sanitized, user-actionable Baofu reasons instead of replacing them with only LocalLife stable copy. Provider text that is not safe for users must still be retained in sanitized internal diagnostics, with classified LocalLife guidance used only as the user-facing fallback. | Focused validation in `5ea8634c`: `go test ./baofu/account -run 'TestAccountClient(ProviderErrorMarksOpenAccountFailureMessagePresent\|OpenAccountResultDiagnosticMarksResultFailureMessagePresent)' -count=1`; `go test ./baofu -run 'TestBaofu(UpstreamMessageSanitizationForRecord\|UserVisibleMessageIncludesActionableProviderReason\|UserVisibleMessageRedactsSensitiveProviderReason\|UserVisibleMessageKeepsDuplicateAccountReasonSafe\|UserVisibleUpstreamReasonRejectsInternalIdentifiers\|CommandMessageRecordsCleanUserActionableReason)' -count=1`; `go test ./logic -run 'TestBaofu(AccountOpeningFailureStatusDescUsesSafeProviderReason\|AccountOnboardingServiceStart_ProviderOpenErrorPersistsSafeFailureGuidance)' -count=1`; `go test ./api -run 'Test(BaofuAccountOpenCallbackPersistsFailureReasonBeforeAck\|BaofuSettlementAccountOperatorFailedFlowReturnsPersistedOpenAccountFailureReason)' -count=1`. | Do not discard Baofu-specific failure reasons. Future Baofu changes must preserve provider code/message for internal diagnosis, return sanitized actionable reasons to the frontend when safe, and only fall back to classified LocalLife guidance when the provider text is not safe for users. |

## Product Decisions Recorded 2026-06-10

These are product decisions captured from the user on 2026-06-10. Each affected slice and the parent audit marks whether implementation/proof is now fixed or still pending where the current runtime does not yet match.

- `merchant-staff-and-group`: staff invite codes must support revoke/rotate, and old codes are invalid immediately. Implemented on 2026-06-10 with explicit rotate/revoke backend routes, single-current-code invalidation through globally unique stored `merchants.bind_code`, conditional get-or-generate persistence for cold concurrent popup opens, Mini Program copy/rotate/revoke/regenerate actions, and API/sqlc/migration regressions. Fixed 2026-06-10: disabled/removed staff may re-apply through a valid invite into pending status with no coarse-role grant until owner confirmation, owner direct-add can reactivate disabled rows with a real role, and group application submit backend-enforces the minimum document set: business license, legal representative ID card front/back, and trademark registration certificate, with Mini Program upload paths aligned.
- `merchant-finance-withdrawal`: withdrawal create must require `Idempotency-Key`; withdrawal callbacks must persist durable callback/fact records before async application; managers may operate withdrawal flows while settlement-account submission remains separately owner-owned unless changed later. Implemented on 2026-06-10 for create/list/detail/balance withdrawal flows, including forward hardening for already-applied weak idempotency-pair schemas, internal Baofu create-error diagnostics in command rows, provider-balance versus local-finance truth-boundary proof, and Mini Program durable-detail terminal-refresh proof.
- `merchant-manual-open-status`: manual open/close temporarily overrides automatic business-hours control until the next business-hours switching point. Implemented on 2026-06-10 through durable `manual_open_status_until`, DB-clock boundary calculation, scheduler skip/cleanup, and payment-invalid fail-closed coverage.
- `merchant-device-display-config`: Flutter BLE printing must honor backend display config and gain observability/deduplication boundaries. Fixed 2026-06-10: Mini Program voice broadcast controls are hidden/removed, persisted backend `voice_*` fields are deprecated/no-op compatibility state until safe cleanup, and Flutter App alert-driven auto-accept reads backend display-config truth instead of a local auto-accept preference.
- `merchant-marketing-rules`: disabled/deleted voucher templates cannot be used for new preview/order, including already issued unused vouchers; existing orders are not retroactively changed. Recharge-rule delete now deactivates rather than physically deletes, preserving historical recharge provenance.
- `merchant-member-balance-adjust`: managers are allowed to manually adjust customer stored-value balance; unused direct generated balance SQL writers are retired.
- `merchant-reservation-and-table`: merchant-created phone/walk-in reservations need an offline/phone customer identity model so no-show behavior does not punish the operator account. Implemented on 2026-06-10 with migration `000264`, `merchant_offline_customers`, normalized contact backfill, `offline_customer_id`, `created_by_user_id`, merchant-scoped offline-customer lookup, edit-time identity repointing, and offline no-show `fact_snapshot` non-attribution while preserving online no-show attribution. Hardened on 2026-06-11 so customer-owner paths and `/v1/reservations/me` totals only treat `NULL`, blank, trim-space, or `online` source rows as user-owned.
- `merchant-claim-recovery`: store managers may handle/create recovery disputes and registered-route proof now covers manager dispute creation; recovery payment remains owner-only.
- `merchant-app-bind-and-device`: App bind code consumption now happens after recheckable preconditions and preserves strict one-time success semantics through final Redis compare-and-delete. Flutter device-sync registration/heartbeat now align to the Android-only backend platform contract, active Flutter logout attempts backend device unregister before clearing local auth, backend stale-device cleanup deactivates 90-day no-heartbeat devices that never receive active logout with `000265` partial-index support, and backend native-push terminal failures now degrade only the affected active device/token row before deactivating that same row after the third consecutive terminal failure. This does not affect the merchant, staff user, or App-wide push capability.
- `merchant-application-onboarding`: doc reconciliation 2026-06-11 verified the Mini Program `getMyApplication()` fallback is no longer stale. `GET /v1/merchants/applications/me` is registered, read-only, and API-tested for no submitted reset and no draft creation. Fixed 2026-06-11: `GET /v1/merchant/application` also no longer resets `submitted` applications on page load. Fixed 2026-06-11: explicit edit/reset now cancels active merchant onboarding review runs as `superseded_by_edit`, refreshes the visible `needs_resubmit` summary, and worker races skip as terminal superseded with summary refresh instead of retrying.

## Open Queue After These Repairs

The highest-risk remaining merchant-side follow-ups are:

1. `merchant-application-onboarding`: submitted page-open reset is closed for `GET /v1/merchant/application`, and explicit edit/reset queued-review supersede visibility is closed. Continue with stale pending OCR cleanup, submit retry idempotency, approved edit semantics, or failed-validation consent audit only as separate follow-ups.
2. `merchant-order-operations`: optional historical reconciliation only if durable command/fact evidence can prove terminal `failed` refund rows were written from retryable/queryable Baofu create errors. Do not auto-retry historical money movement by status alone.
3. `merchant-finance-withdrawal`: withdrawal `Idempotency-Key`, already-applied schema hardening, durable callback/fact persistence before async application, internal Baofu create-error diagnostics, manager withdrawal operation permission, provider-balance versus local-finance truth-boundary proof, Mini Program accepted/unknown -> durable detail -> terminal refresh proof, and settlement-account submit/status long-wait UI contract proof are closed. Remaining: decide whether to redesign the pre-existing local-row-before-provider-call outbox gap, clarify provider `returned` merchant-facing behavior, and optionally add broader provider-timeout E2E proof.
4. `merchant-claim-recovery`: manager dispute permission is product-approved and now has explicit registered-route regression coverage. Remaining: decide claim-id compatibility route for external clients and expose merchant-facing delayed-release visibility.
5. `merchant-member-balance-adjust`: manager stored-value mutation is product-approved and has explicit registered-route regression coverage; unused direct balance SQL writers are retired and guarded. Continue only for optional product/API work such as returning a manual-adjustment `transaction_id`.
6. `merchant-membership-settings`: scene contract drift is closed as `dine_in/takeaway` only, PUT optional-field semantics are closed as partial merge over existing settings/defaults with explicit empty arrays preserved, direct backend `takeaway` balance order creation has API proof, the observed dine-in checkout balance selector follows backend payment assessment, rules-engine scene parity is covered, owner GET/PUT API contract plus request validation are covered, and clean/dirty/weak-schema migration fixture proof for `000252/000253` is covered. Mini Program `takeaway` balance checkout reachability and preview/direct consistency are now closed by `check:takeaway-balance-checkout`.
7. `merchant-marketing-rules`: voucher quantity-update product error mapping, voucher-template disabled/deleted/not-current blocking for future list/preview/order use, recharge-rule delete-as-deactivate provenance preservation, discount-rule and delivery-promotion update effective validation, delivery-promotion DB direct-writer constraints, discount `/applicable` plus `/best` static-route reachability, voucher/discount full-count pagination truth, order-preview typed cart lookup, and cart/order preview/direct-create marketing total parity are proof-covered. Remaining: optional broader save-to-consumer proof for recharge bonus resolution and voucher rollback; Mini Program voucher/discount list probe cleanup remains optional compatibility cleanup.
8. `merchant-device-display-config`: cloud-printer deletion/history-log drift is closed by soft delete and focused DB/API/worker validation, including historical status over soft-deleted printers, retry failure logging, Yilianyun rebind, active-SN re-registration, and duplicate-SN migration down. Mini Program voice controls and backend `voice_*` PUT no-op compatibility are closed. Display-config `enable_print=false` now forces `auto_accept_paid_orders=false` across backend write, DB constraint/migration, and Mini Program draft/payload boundaries. Flutter App alert-driven auto-accept now reads backend display-config truth and fails closed on config read failure/timeout. Remaining: merchant-facing reconciliation UI or wrapper cleanup and Flutter BLE backend-config/observability/dedupe implementation.
9. `merchant-dish-status-and-inventory`: the known dish/inventory repair items from this slice are closed on the Mini Program/backend paths covered here, including the `UpdateDishAvailability` zombie SQL cleanup. Only a future product/architecture decision to collapse the three-step dish edit into one backend workflow remains optional.
10. `merchant-combo-and-catalog`: category sort-only persistence, combo child availability consistency across write/public-read/customer-action paths, combo delete soft-delete contract documentation, category hidden-reference blocking across delete/rename, merchant combo tag permission alignment, and direct combo-dish endpoint retirement are closed. Continue this slice only if new evidence appears.
11. `merchant-review-reply`: owner-only page access, hidden-review reply visibility proof, current reply overwrite/timestamp semantics, merchant all-review image resolver proof, and Mini Program missing-OpenID/content-safety failure copy are closed. Continue only with optional reply history/clear/versioning/idempotent-replay product decisions or a product request for merchant-visible pending review images.
12. `merchant-profile-update`: merchant category replacement, live shop-image migration to `merchants`, migration `000250` hardening for already-applied weaker `000249` schemas, search/category merchant-live image preference, Logo conflict rollback/user guidance, and storefront/environment pending-sync recovery proof are closed. Continue this slice only for future compatibility-fallback retirement or duplicated onboarding API cleanup.
13. `merchant-reservation-and-table`: backend `date+status` reservation list total semantics, table create/update tag transactionality, table image media binding validation, table-bound set-primary/delete image actions, locked table delete branches, future-reservation table update/disable blocking, reservation-create locked-table revalidation, merchant reservation-edit locked-table/time-conflict revalidation, reservation-to-dining-session open/close convergence, manual table release active-session close routing, Mini Program status-copy contract, Flutter table media-category alignment, Mini Program action-permission/date+status total-consumption contract, Mini Program partial table-image recovery, Flutter table flow/websocket proof, offline/phone customer identity/no-show non-attribution, and non-online operator denial across customer owner/count/payment/order/session paths are now proof-covered. Continue this slice only for separate optional product choices such as terminal action idempotent replay unless new evidence appears.
14. `merchant-staff-and-group`: pending staff role-aware authorization, disabled-staff pending re-application plus owner-confirmed reactivation, `GetMerchantByOwner`/logic fallback filtering, staff/coarse-role transaction ownership, staff invite-code revoke/rotate with immediate invalidation plus cold get-or-generate concurrency and global stored-code uniqueness, typed duplicate-key classification, direct group license image validation, group OCR JSON concurrency, group submit minimum-document completeness, group join create/reject/cancel atomic audit plus cancel race, pending-only join uniqueness, and Mini Program group-join duplicate-submit/re-entry recovery are closed. Continue only for separate group-contract drift candidates, such as group review route alias cleanup or applicant-side group-join cancellation UI if product wants it.
15. `merchant-app-bind-and-device`: consume-after-recheckable-preconditions bind-code decision, Flutter Android-only platform payload contract, active logout unregister, backend stale-device cleanup, and backend per-device native-push terminal-failure degradation are closed with DB/scheduler/index/migration proof. Continue this slice only if product wants verify-time device metadata persisted before App registration, or if provider-specific native push SDK error classifiers are added later.

## Maintenance Rule

Every future repair from the merchant-state-flow audit must update all three places in the same branch:

1. This remediation log.
2. The matching row and next-step status in `artifacts/merchant-state-flow-audit.md`.
3. The matching `artifacts/codegraph/merchant-state-flows/<flow-id>.slice.md`.
