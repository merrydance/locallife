# 小程序端开发任务进度表 (mp-dev)

> **目标**：补齐商户/骑手/运营三端功能，标准化 TDesign UI
> 规范，打通全链路业务闭环。

---

## 1. 核心业务链路补齐

- [x] **下单链路闭环**：实现 `api/order.ts` 中的 `createOrderFromCart`
      逻辑，对接购物车与结算页。
- [ ] **支付状态同步**：优化支付成功后的跳转与状态更新逻辑，确保支付回调与前端
      UI 同步。
- [x] **评价系统**：补齐 `pages/user_center/reviews` 的创建与展示功能。

## 2. 商户管理端 (缺失模块)

- [x] **商户工作台首页**：创建
      `pages/merchant/dashboard`，展示今日营业额、订单数、待处理任务提醒。
- [x] **商品/库存管理**：实现菜品上架/下架、临时估清（库存秒改）功能。
- [x] **房型/桌台管理**：针对堂食/预订，提供桌台状态查看与手动释放功能。
- [x] **商户订单管理**：商户侧的接单、拒单、核销（预定/堂食）功能。
- [x] **数据简报**：基础的营收汇总与热销菜品排行。

## 3. 骑手端增强

- [x] **WebSocket
      实时推送**：集成实时通知，让骑手在“抢单大厅”无需手动刷新即可获取新单提醒。
- [x] **物理距离校验**：在抢单前增加 LBS 校验，确保骑手在规定范围内。
- [x] **异常申诉流程**：完成 `pages/rider/exception` 的上报与后续逻辑。

## 4. 运营与平台端建议

- [x] **高级规则编辑器**：在 `pages/operator/rules`
      增加可视化配置界面，而非简单的列表展示。
- [ ] **区域管理详情**：细化运营商对该区县下商户、骑手的管理权限。

## 5. UI/UX 规范化 (TDesign 统一化)

- [x] **重构扫码模块**：将 `pages/dine-in/scan-entry` 里的原生组件替换为 TDesign
      规范组件。
- [x] **统一骨架屏**：所有具备列表加载的页面统一使用 `t-skeleton` 代替自定义 CSS
      骨架。
- [ ] **异常状态检查**：确保所有 subPackages
      内的页面都具备重试机制与空状态视图。

---

## 执行记录廊

- **2026-02-10**: 完成商户管理端全量基础模块 (Dashboard, Dishes, Orders,
  Tables)，并补齐部分骑手端 WebSocket 自动刷新机制。
