// Protected tombstone module.
// Operator console owners were split into task-domain services:
// - services/operator-regions.ts
// - services/operator-analytics-dashboard.ts
// - services/operator-workbench.ts
// Keep this file only as a protected boundary so future code cannot silently rebuild the old super-service import path.

export {}
