<custom-navbar title="商户管理" show-back="{{true}}" show-location="{{false}}" bind:navheight="onNavHeight" />

<view
  class="merchants-page page-shell page-shell--with-nav page-shell--bottom-safe page-shell--page-gutter"
  style="--page-shell-nav-height: {{navBarHeight}}px"
>
  <scroll-view
    class="list-scroll"
    scroll-y
    refresher-enabled
    refresher-triggered="{{refreshing}}"
    bindrefresherrefresh="onRefresh"
    bindscrolltolower="onLoadMore"
  >
    <view class="merchants-container">
      <view class="summary-card">
        <text class="summary-title">商户总数</text>
        <text class="summary-value">{{total}}</text>
      </view>

      <view wx:if="{{loading}}" class="section-card">
        <view wx:for="{{3}}" wx:key="index" class="skeleton-item skeleton-shimmer"></view>
      </view>

      <view wx:if="{{error}}" class="section-card status-wrap">
        <t-empty icon="error-circle" description="{{error}}">
          <t-button slot="action" theme="primary" bind:tap="onRetry">重试</t-button>
        </t-empty>
      </view>

      <view wx:if="{{!loading && !error && merchants.length === 0}}" class="section-card status-wrap">
        <t-empty description="暂无商户" />
      </view>

      <view wx:for="{{merchants}}" wx:key="id" class="section-card merchant-card" bind:tap="onDetailTap" data-id="{{item.id}}">
        <view class="title-row">
          <view class="name">{{item.name || ('商户#' + item.id)}}</view>
          <t-tag theme="{{item.statusTheme}}" variant="light">{{item.statusLabel}}</t-tag>
        </view>
        <view class="meta-row">
          <text class="meta-label">区县</text>
          <text class="meta-value">{{item.regionText}}</text>
        </view>
        <view class="meta-row">
          <text class="meta-label">营业状态</text>
          <text class="meta-value">{{item.openLabel}}</text>
        </view>
        <view class="meta-row">
          <text class="meta-label">近一月订单</text>
          <text class="meta-value">{{item.monthOrdersText}}</text>
        </view>
        <view class="meta-row">
          <text class="meta-label">被投诉</text>
          <text class="meta-value">{{item.complaintText}}</text>
        </view>
      </view>

      <view wx:if="{{!loading && !hasMore && merchants.length > 0}}" class="no-more">没有更多了</view>
    </view>
  </scroll-view>
</view>
