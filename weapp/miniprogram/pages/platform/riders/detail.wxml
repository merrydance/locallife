<custom-navbar title="骑手详情" show-back="{{true}}" show-location="{{false}}" bind:navheight="onNavHeight" />

<view
  class="detail-page page-shell page-shell--with-nav page-shell--bottom-safe page-shell--page-gutter"
  style="--page-shell-nav-height: {{navBarHeight}}px"
>
  <scroll-view class="detail-scroll" scroll-y>
    <view class="detail-container">
      <view wx:if="{{loading}}" class="section-card">
        <view wx:for="{{4}}" wx:key="index" class="skeleton-item skeleton-shimmer"></view>
      </view>

      <view wx:elif="{{error}}" class="section-card status-wrap">
        <t-empty icon="error-circle" description="{{error}}">
          <t-button slot="action" theme="primary" bind:tap="onRetry">重试</t-button>
        </t-empty>
      </view>

      <view wx:elif="{{!detail}}" class="section-card status-wrap">
        <t-empty description="未找到骑手详情" />
      </view>

      <block wx:else>
        <view class="section-card">
          <view class="title-row">
            <text class="title">{{detail.name || ('骑手#' + detail.id)}}</text>
            <t-tag theme="{{detail.activeTheme}}" variant="light">{{detail.activeLabel}}</t-tag>
          </view>
          <view class="meta-row">
            <text class="meta-label">区县</text>
            <text class="meta-value">{{detail.regionText}}</text>
          </view>
          <view class="meta-row">
            <text class="meta-label">年龄</text>
            <text class="meta-value">{{detail.ageText}}</text>
          </view>
          <view class="meta-row">
            <text class="meta-label">性别</text>
            <text class="meta-value">{{detail.genderText}}</text>
          </view>
          <view class="meta-row">
            <text class="meta-label">接单状态</text>
            <text class="meta-value">{{detail.statusLabel}}</text>
          </view>
        </view>

        <view class="section-card">
          <view class="section-title">接单统计</view>
          <view class="metric-grid">
            <view class="metric-item">
              <text class="metric-label">接单总数</text>
              <text class="metric-value">{{detail.order_stats.total_orders}}</text>
            </view>
            <view class="metric-item">
              <text class="metric-label">总收入</text>
              <text class="metric-value">{{detail.totalIncomeText}}</text>
            </view>
            <view class="metric-item">
              <text class="metric-label">近一个月接单</text>
              <text class="metric-value">{{detail.order_stats.last_month_orders}}</text>
            </view>
            <view class="metric-item">
              <text class="metric-label">近一个月收入</text>
              <text class="metric-value">{{detail.lastMonthIncomeText}}</text>
            </view>
          </view>
        </view>

        <view class="section-card">
          <view class="section-title">服务水平</view>
          <view class="meta-row">
            <text class="meta-label">被投诉</text>
            <text class="meta-value">{{detail.complaintText}}</text>
          </view>
          <view wx:for="{{detail.categories}}" wx:key="category" class="meta-row">
            <text class="meta-label">{{item.category}}</text>
            <text class="meta-value">{{item.countText}}</text>
          </view>
          <view wx:if="{{detail.categories.length === 0}}" class="empty-line">暂无投诉分类</view>
        </view>

        <view wx:if="{{actionResultText}}" class="section-card action-result">
          <text class="action-result__title">{{actionResultText}}</text>
          <text class="action-result__note">{{actionResultNote}}</text>
        </view>

        <view wx:if="{{detail.can_pause_accepting || detail.can_resume_accepting}}" class="section-card">
          <t-button
            theme="{{detail.can_pause_accepting ? 'danger' : 'primary'}}"
            block
            loading="{{submitting}}"
            bind:tap="onToggleAccepting"
          >{{detail.can_pause_accepting ? '暂停接单' : '恢复接单'}}</t-button>
        </view>
      </block>
    </view>
  </scroll-view>
</view>
