<custom-navbar title="订单详情" showBack="{{true}}" bind:navheight="onNavHeight" />

<view class="page-container" style="padding-top: {{navBarHeight}}px; padding-bottom: calc(120rpx + env(safe-area-inset-bottom));">
  <t-loading wx:if="{{loading}}" theme="circular" size="40rpx" class="loading" />

  <view wx:if="{{!loading && order}}" class="detail-content animate-fade-in">
    <!-- 1. 顶部状态区 -->
    <view class="status-header">
      <view class="status-left">
        <view class="status-title">{{order.statusText}}</view>
        <view wx:if="{{order.expectDeliverTime}}" class="status-desc">预计 {{order.expectDeliverTime}} 送达</view>
        <view wx:elif="{{order.status === 'completed'}}" class="status-desc">感谢您对{{order.merchantName}}的信任</view>
        <view wx:elif="{{order.status === 'cancelled'}}" class="status-desc">{{order.cancelReason || '订单已取消'}}</view>
        <view wx:else class="status-desc">订单编号: {{order.orderNo}}</view>
      </view>
      <t-icon name="{{order.status === 'completed' ? 'check-circle' : 'time'}}" size="56rpx" color="#333" />
    </view>

    <!-- 2. 操作引导区 (如去支付/物流) -->
    <view wx:if="{{showPayButton || showTrackingButton}}" class="action-card card">
       <view class="action-content">
          <view class="action-text">
            <text wx:if="{{showPayButton}}">订单等待支付，请在15分钟内完成</text>
            <text wx:if="{{showTrackingButton}}">骑手正在为您配送中</text>
          </view>
          <t-button wx:if="{{showPayButton}}" theme="primary" size="small" shape="round" bindtap="onPayOrder">立即支付</t-button>
          <t-button wx:if="{{showTrackingButton}}" variant="outline" size="small" shape="round" bindtap="onViewTracking">查看进度</t-button>
       </view>
    </view>

    <!-- 3. 商品明细卡片 -->
    <view class="order-card card">
      <!-- 商户头 -->
      <view class="merchant-header" bindtap="onEnterMerchant" data-id="{{order.merchantId}}" hover-class="hover-opacity">
        <view class="merchant-info">
           <text class="merchant-name">{{order.merchantName}}</text>
           <t-icon name="chevron-right" size="32rpx" color="#999" />
        </view>
        <t-icon name="call" size="40rpx" catchtap="onCallMerchant" color="var(--td-brand-color)" />
      </view>
      
      <t-divider />

      <!-- 商品列表 -->
      <view class="item-list">
        <view wx:for="{{order.items}}" wx:key="id" class="order-item">
          <t-image src="{{item.imageUrl}}" mode="aspectFill" width="112rpx" height="112rpx" shape="round" class="item-img" />
          <view class="item-main">
            <view class="item-row-1">
              <view class="item-name">{{item.name}}</view>
              <view class="item-price">{{item.unitPriceDisplay}}</view>
            </view>
            <view class="item-row-2">
              <view class="item-specs">
                <text wx:for="{{item.customizations}}" wx:key="*this" wx:for-item="cust" class="spec-tag">{{cust}}</text>
              </view>
              <view class="item-qty">x{{item.quantity}}</view>
            </view>
          </view>
        </view>
      </view>

      <t-divider dashed />

      <!-- 费用详情 -->
      <view class="fee-list">
        <view class="fee-row" wx:if="{{order.type === 'takeout'}}">
           <text>配送费</text>
           <view class="price-group">
             <text wx:if="{{order.deliveryFeeDiscount}}" class="discount-price">{{order.deliveryFeeDiscountDisplay}}</text>
             <text>{{order.deliveryFeeDisplay}}</text>
           </view>
        </view>
        <view class="fee-row" wx:if="{{order.discountAmount > 0}}">
           <text>优惠合计</text>
           <text class="highlight-red">{{order.discountAmountDisplay}}</text>
        </view>
      </view>

      <view class="total-row">
         <text class="total-label">实付</text>
         <text class="total-price">{{order.payableAmountDisplay}}</text>
      </view>
    </view>

    <!-- 4. 配送/预订信息卡片 -->
    <view class="info-card card">
      <view class="card-title">配送信息</view>
      
      <t-cell-group>
        <block wx:if="{{order.type === 'takeout'}}">
          <t-cell title="送达地址" note="{{order.address}}" align="top" t-class-note="address-note" />
          <t-cell title="联系人" note="{{order.contactName}} {{order.contactPhone}}" />
          <t-cell title="配送时间" note="{{order.expectDeliverTime || '立即配送'}}" />
        </block>

        <block wx:if="{{order.type === 'reservation'}}">
           <t-cell title="预订桌台" note="{{reservationInfo.table_no || '待分配'}}" />
           <t-cell title="预订时间" note="{{reservationInfo.reservation_date}} {{reservationInfo.reservation_time}}" />
           <t-cell title="预订人数" note="{{reservationInfo.guest_count}}人" />
           <t-cell title="预订人" note="{{reservationInfo.contact_name}} {{reservationInfo.contact_phone}}" />
        </block>

         <block wx:if="{{order.type === 'dine_in'}}">
           <t-cell title="就餐桌号" note="#{{order.tableId || '--'}}" />
           <t-cell title="就餐人数" note="{{order.guestCount || '--'}}人" />
         </block>
      </t-cell-group>
    </view>

    <!-- 5. 订单信息卡片 -->
    <view class="info-card card">
      <view class="card-title">订单信息</view>
      <t-cell-group>
        <t-cell title="订单号码" note="{{order.orderNo}}" hover arrow bindtap="onCopy" data-text="{{order.orderNo}}" />
        <t-cell title="下单时间" note="{{order.createTime}}" />
        <t-cell title="支付方式" note="{{order.paymentMethod || '在线支付'}}" />
        <t-cell wx:if="{{order.notes}}" title="备注" note="{{order.notes}}" align="top" />
      </t-cell-group>
    </view>

  </view>
</view>

<!-- 底部固定操作栏 -->
<view wx:if="{{!loading && order}}" class="bottom-bar">
   <view class="bottom-actions">
      <!-- 辅助按钮 -->
      <view class="secondary-actions">
         <t-button wx:if="{{showCancelButton}}" size="medium" variant="outline" shape="round" bindtap="onCancelOrder">取消订单</t-button>
         <t-button wx:if="{{showContactButton}}" size="medium" variant="outline" icon="chat" shape="round" bindtap="onContact">联系客服</t-button>
      </view>

      <!-- 主要按钮 -->
      <view class="primary-actions">
         <t-button wx:if="{{showPayButton}}" theme="primary" size="medium" shape="round" bindtap="onPayOrder">去支付</t-button>
         <t-button wx:if="{{showUrgeButton}}" theme="primary" variant="outline" size="medium" shape="round" bindtap="onUrgeOrder" disabled="{{urgeCountdown > 0}}">{{urgeCountdown > 0 ? urgeCountdown + 's' : '催单'}}</t-button>
         <t-button wx:if="{{showConfirmButton}}" theme="primary" size="medium" shape="round" bindtap="onConfirmReceipt">确认收货</t-button>
         <t-button wx:if="{{order.status === 'completed'}}" theme="primary" size="medium" shape="round" bindtap="onReorder">再来一单</t-button>
      </view>
   </view>
</view>
