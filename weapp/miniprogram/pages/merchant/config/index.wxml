<custom-navbar title="配置中心" showBack="{{true}}" />

<view class="page-container" style="padding-top: {{navBarHeight}}px">
  <view class="tips">商户所有配置项统一在此维护</view>
  <t-cell-group theme="card">
    <t-cell
      wx:for="{{configItems}}"
      wx:key="id"
      title="{{item.title}}"
      description="{{item.desc}}"
      arrow
      data-path="{{item.path}}"
      bind:tap="onTapItem"
    />
  </t-cell-group>
</view>
