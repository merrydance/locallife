<custom-navbar title="叠加规则" showBack="{{true}}" />

<view
  class="page-container page-shell page-shell--with-nav page-shell--bottom-safe page-shell--page-gutter"
  style="--page-shell-nav-height: {{navBarHeight}}px;"
>
  <view wx:if="{{accessReady && accessDenied}}" class="state-block">
    <t-empty icon="error-circle" description="当前账号无商户权限，请返回“我的”切换身份" />
  </view>

  <view wx:elif="{{accessReady && accessErrorMessage}}" class="state-block">
    <t-empty icon="error-circle" description="{{accessErrorMessage}}" />
    <t-button theme="primary" bindtap="onRetryAccess">重新校验</t-button>
  </view>

  <block wx:else>
  <view wx:if="{{initialLoading}}" class="state-block">
    <t-loading theme="circular" size="40rpx" text="加载会员设置中..." vertical />
  </view>

  <view wx:elif="{{initialError}}" class="state-block">
    <t-empty description="{{initialErrorMessage || '会员设置加载失败，请重试'}}" />
    <t-button theme="primary" bindtap="onRetry">重试</t-button>
  </view>

  <block wx:else>
    <view class="page-content">
      <t-button wx:if="{{refreshErrorMessage}}" class="notice-action" theme="primary" bindtap="onRetryRefresh">重试</t-button>

      <merchant-section-card title="叠加规则" bodyPadding="none">
        <t-cell-group>
          <t-cell title="允许与优惠券叠加" description="开启后，会员余额或赠送金可与平台/店铺优惠券同时使用">
            <t-switch slot="note" value="{{form.allow_with_voucher}}" data-field="allow_with_voucher" bind:change="onToggleRule" size="large" />
          </t-cell>
          <t-cell title="允许与折扣活动叠加" description="开启后，会员权益可与店铺折扣活动同时使用">
            <t-switch slot="note" value="{{form.allow_with_discount}}" data-field="allow_with_discount" bind:change="onToggleRule" size="large" />
          </t-cell>
          <t-input label="最高抵扣比例" type="number" value="{{form.max_deduction_percent}}" placeholder="1-100" bindchange="onPercentChange">
            <text slot="suffix">%</text>
          </t-input>
        </t-cell-group>
      </merchant-section-card>

      <merchant-section-card title="余额可用场景">
        <view class="scene-list">
          <view wx:for="{{sceneOptions}}" wx:key="key" class="scene-card {{form.balanceScenes[item.key] ? 'scene-card-active' : ''}}" data-group="balanceScenes" data-scene="{{item.key}}" bindtap="onToggleScene">
            <view wx:if="{{form.balanceScenes[item.key]}}" class="scene-mark">
              <t-icon name="check-circle-filled" size="28rpx" color="var(--td-brand-color)" />
            </view>
            <view class="scene-head">
              <text class="scene-title">{{item.label}}</text>
              <text class="scene-check">{{form.balanceScenes[item.key] ? '已启用' : '未启用'}}</text>
            </view>
            <text class="scene-desc">{{item.desc}}</text>
          </view>
        </view>
      </merchant-section-card>

      <merchant-section-card title="赠送金可用场景">
        <view class="scene-list">
          <view wx:for="{{sceneOptions}}" wx:key="key" class="scene-card {{form.bonusScenes[item.key] ? 'scene-card-active' : ''}}" data-group="bonusScenes" data-scene="{{item.key}}" bindtap="onToggleScene">
            <view wx:if="{{form.bonusScenes[item.key]}}" class="scene-mark">
              <t-icon name="check-circle-filled" size="28rpx" color="var(--td-brand-color)" />
            </view>
            <view class="scene-head">
              <text class="scene-title">{{item.label}}</text>
              <text class="scene-check">{{form.bonusScenes[item.key] ? '已启用' : '未启用'}}</text>
            </view>
            <text class="scene-desc">{{item.desc}}</text>
          </view>
        </view>
      </merchant-section-card>

      <view class="form-actions">
        <t-button theme="primary" loading="{{saving}}" disabled="{{saving || !hasChanges}}" bindtap="onSave">保存会员设置</t-button>
      </view>
    </view>
  </block>
  </block>
  <t-toast id="t-toast" />
</view>