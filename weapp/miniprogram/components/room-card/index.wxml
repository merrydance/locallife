<view class="room-card" bindtap="onTap">
  <t-image class="room-image" src="{{room.imageUrl}}" mode="aspectFill" lazy />
  
  <view class="room-info">
    <!-- 第1行: 餐厅名·包间名 + 价格 -->
    <view class="row-1">
      <text class="restaurant-room-name">{{room.restaurantName}}·{{room.name}}</text>
      <text class="price">{{room.priceDisplay}}起</text>
    </view>
    
    <!-- 第2行: 容纳人数 -->
    <view class="row-2">
      <text class="capacity">{{room.capacity.min}}-{{room.capacity.max}}人</text>
    </view>
    
    <!-- 第3行: 设施标签 -->
    <view class="row-3">
      <t-tag wx:for="{{room.facilities}}" wx:key="*this" size="small">{{item}}</t-tag>
    </view>
    
    <!-- 第4行: 评分·预订次数 -->
    <view class="row-4">
      <text class="rating">⭐{{room.ratingDisplay}}</text>
      <text class="dot">·</text>
      <text class="booking">{{room.bookingBadge}}</text>
    </view>
    
    <!-- 第5行: 地址 -->
    <view class="row-5">
      <text class="address">{{room.address}}</text>
    </view>
    
    <!-- 第6行: 距离·营业时间 -->
    <view class="row-6">
      <text class="distance">📍{{room.distance}}</text>
      <text class="dot">·</text>
      <text class="hours">🕐营业中</text>
    </view>
  </view>
</view>
