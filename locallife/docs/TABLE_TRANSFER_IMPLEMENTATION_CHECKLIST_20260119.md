# 商户侧转台（换桌）实施步骤清单

日期：2026-01-19

> 说明：无需额外确认即可推进。以下为可执行清单（完成后勾选）。

## 0. 决策与范围锁定
- [x] 开放 C 端扫码转台（需用户归属校验）
- [x] 不引入 `orders.dining_session_id`（使用账单组链路）
- [x] 不新增桌台状态 `maintenance/cleaning`

## 1. 数据库与迁移
- [x] 新增转台日志表 `table_transfer_logs`
- [ ] 更新 SQLC 查询与模型生成

## 2. 后端接口与事务
- [x] 新增接口 `POST /v1/dining-sessions/{id}/transfer-table`
- [x] 增加权限校验：商户员工 + 会话所属用户
- [x] 实现转台事务（锁会话/桌台、校验目标桌、更新会话/订单/预订/桌台状态）
- [x] 写入转台日志
- [x] WebSocket 推送 `table_transfer` 事件

## 3. 业务规则与校验
- [x] 目标桌必须同商户、非停用、无开放会话
- [x] 目标桌在当前时段不可被其他预订占用
- [x] 预订会话需校验预订归属
- [x] 非预订场景需校验新桌 `table_code`

## 4. 订单同步
- [x] 通过 `billing_groups(dining_session_id)` + `billing_group_orders` 获取会话订单
- [x] 批量更新这些订单的 `table_id = to_table_id`

## 5. 商户端小程序
- [ ] 桌台列表增加“转台”入口
- [ ] 转台选择页：过滤可用桌台
- [ ] 提交后刷新桌台状态与会话信息

## 6. C 端扫码（可选）
- [x] 扫码检测到已有会话/预订弹窗确认
- [x] 确认后调用转台接口
- [x] 失败提示与回退策略

## 7. 文档与监控
- [x] 更新接口文档（swagger）
- [ ] 监控转台失败率/冲突率

## 8. 测试用例
- [x] 无预订转台成功
- [x] 有预订转台成功
- [x] 目标桌已占用/被预订/停用
- [x] 会话已关闭
- [x] 并发转台
- [x] 订单台号同步正确

## 9. 发布计划
- [ ] 灰度开关
- [ ] 数据回填策略（如新增字段）
- [ ] 回滚方案
