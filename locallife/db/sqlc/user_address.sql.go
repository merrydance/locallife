// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: user_address.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countUserAddresses = `-- name: CountUserAddresses :one
SELECT COUNT(*) FROM user_addresses
WHERE user_id = $1
`

func (q *Queries) CountUserAddresses(ctx context.Context, userID int64) (int64, error) {
	row := q.db.QueryRow(ctx, countUserAddresses, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createUserAddress = `-- name: CreateUserAddress :one
INSERT INTO user_addresses (
  user_id,
  region_id,
  detail_address,
  contact_name,
  contact_phone,
  longitude,
  latitude,
  is_default
) VALUES (
  $1, $2, $3, $4, $5, $6, $7, $8
) RETURNING id, user_id, region_id, detail_address, contact_name, contact_phone, longitude, latitude, is_default, created_at
`

type CreateUserAddressParams struct {
	UserID        int64          `json:"user_id"`
	RegionID      int64          `json:"region_id"`
	DetailAddress string         `json:"detail_address"`
	ContactName   string         `json:"contact_name"`
	ContactPhone  string         `json:"contact_phone"`
	Longitude     pgtype.Numeric `json:"longitude"`
	Latitude      pgtype.Numeric `json:"latitude"`
	IsDefault     bool           `json:"is_default"`
}

func (q *Queries) CreateUserAddress(ctx context.Context, arg CreateUserAddressParams) (UserAddress, error) {
	row := q.db.QueryRow(ctx, createUserAddress,
		arg.UserID,
		arg.RegionID,
		arg.DetailAddress,
		arg.ContactName,
		arg.ContactPhone,
		arg.Longitude,
		arg.Latitude,
		arg.IsDefault,
	)
	var i UserAddress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RegionID,
		&i.DetailAddress,
		&i.ContactName,
		&i.ContactPhone,
		&i.Longitude,
		&i.Latitude,
		&i.IsDefault,
		&i.CreatedAt,
	)
	return i, err
}

const deleteUserAddress = `-- name: DeleteUserAddress :exec
DELETE FROM user_addresses
WHERE id = $1 AND user_id = $2
`

type DeleteUserAddressParams struct {
	ID     int64 `json:"id"`
	UserID int64 `json:"user_id"`
}

func (q *Queries) DeleteUserAddress(ctx context.Context, arg DeleteUserAddressParams) error {
	_, err := q.db.Exec(ctx, deleteUserAddress, arg.ID, arg.UserID)
	return err
}

const getUserAddress = `-- name: GetUserAddress :one
SELECT id, user_id, region_id, detail_address, contact_name, contact_phone, longitude, latitude, is_default, created_at FROM user_addresses
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetUserAddress(ctx context.Context, id int64) (UserAddress, error) {
	row := q.db.QueryRow(ctx, getUserAddress, id)
	var i UserAddress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RegionID,
		&i.DetailAddress,
		&i.ContactName,
		&i.ContactPhone,
		&i.Longitude,
		&i.Latitude,
		&i.IsDefault,
		&i.CreatedAt,
	)
	return i, err
}

const getUserDefaultAddress = `-- name: GetUserDefaultAddress :one
SELECT id, user_id, region_id, detail_address, contact_name, contact_phone, longitude, latitude, is_default, created_at FROM user_addresses
WHERE user_id = $1 AND is_default = true
LIMIT 1
`

func (q *Queries) GetUserDefaultAddress(ctx context.Context, userID int64) (UserAddress, error) {
	row := q.db.QueryRow(ctx, getUserDefaultAddress, userID)
	var i UserAddress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RegionID,
		&i.DetailAddress,
		&i.ContactName,
		&i.ContactPhone,
		&i.Longitude,
		&i.Latitude,
		&i.IsDefault,
		&i.CreatedAt,
	)
	return i, err
}

const listUserAddresses = `-- name: ListUserAddresses :many
SELECT id, user_id, region_id, detail_address, contact_name, contact_phone, longitude, latitude, is_default, created_at FROM user_addresses
WHERE user_id = $1
ORDER BY is_default DESC, created_at DESC
`

func (q *Queries) ListUserAddresses(ctx context.Context, userID int64) ([]UserAddress, error) {
	rows, err := q.db.Query(ctx, listUserAddresses, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []UserAddress{}
	for rows.Next() {
		var i UserAddress
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.RegionID,
			&i.DetailAddress,
			&i.ContactName,
			&i.ContactPhone,
			&i.Longitude,
			&i.Latitude,
			&i.IsDefault,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setAddressAsDefault = `-- name: SetAddressAsDefault :one
UPDATE user_addresses
SET is_default = true
WHERE id = $1 AND user_id = $2
RETURNING id, user_id, region_id, detail_address, contact_name, contact_phone, longitude, latitude, is_default, created_at
`

type SetAddressAsDefaultParams struct {
	ID     int64 `json:"id"`
	UserID int64 `json:"user_id"`
}

// 设置指定地址为默认
func (q *Queries) SetAddressAsDefault(ctx context.Context, arg SetAddressAsDefaultParams) (UserAddress, error) {
	row := q.db.QueryRow(ctx, setAddressAsDefault, arg.ID, arg.UserID)
	var i UserAddress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RegionID,
		&i.DetailAddress,
		&i.ContactName,
		&i.ContactPhone,
		&i.Longitude,
		&i.Latitude,
		&i.IsDefault,
		&i.CreatedAt,
	)
	return i, err
}

const setDefaultAddress = `-- name: SetDefaultAddress :exec
UPDATE user_addresses
SET is_default = false
WHERE user_id = $1
`

// 先将用户的所有地址设为非默认
func (q *Queries) SetDefaultAddress(ctx context.Context, userID int64) error {
	_, err := q.db.Exec(ctx, setDefaultAddress, userID)
	return err
}

const updateUserAddress = `-- name: UpdateUserAddress :one
UPDATE user_addresses
SET
  region_id = COALESCE($1, region_id),
  detail_address = COALESCE($2, detail_address),
  contact_name = COALESCE($3, contact_name),
  contact_phone = COALESCE($4, contact_phone),
  longitude = COALESCE($5, longitude),
  latitude = COALESCE($6, latitude),
  is_default = COALESCE($7, is_default)
WHERE id = $8 AND user_id = $9
RETURNING id, user_id, region_id, detail_address, contact_name, contact_phone, longitude, latitude, is_default, created_at
`

type UpdateUserAddressParams struct {
	RegionID      pgtype.Int8    `json:"region_id"`
	DetailAddress pgtype.Text    `json:"detail_address"`
	ContactName   pgtype.Text    `json:"contact_name"`
	ContactPhone  pgtype.Text    `json:"contact_phone"`
	Longitude     pgtype.Numeric `json:"longitude"`
	Latitude      pgtype.Numeric `json:"latitude"`
	IsDefault     pgtype.Bool    `json:"is_default"`
	ID            int64          `json:"id"`
	UserID        int64          `json:"user_id"`
}

func (q *Queries) UpdateUserAddress(ctx context.Context, arg UpdateUserAddressParams) (UserAddress, error) {
	row := q.db.QueryRow(ctx, updateUserAddress,
		arg.RegionID,
		arg.DetailAddress,
		arg.ContactName,
		arg.ContactPhone,
		arg.Longitude,
		arg.Latitude,
		arg.IsDefault,
		arg.ID,
		arg.UserID,
	)
	var i UserAddress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RegionID,
		&i.DetailAddress,
		&i.ContactName,
		&i.ContactPhone,
		&i.Longitude,
		&i.Latitude,
		&i.IsDefault,
		&i.CreatedAt,
	)
	return i, err
}
