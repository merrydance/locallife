// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: app_version.sql

package db

import (
	"context"
)

const getLatestActiveAppVersion = `-- name: GetLatestActiveAppVersion :one
SELECT id, platform, channel, package_name, version_code, version_name, download_url, changelog, is_force, file_size_bytes, checksum_sha256, status, published_at, created_at, updated_at
FROM app_versions
WHERE platform = $1
  AND channel = $2
  AND package_name = $3
  AND version_code > $4
  AND status = 'active'
  AND published_at <= now()
ORDER BY version_code DESC, published_at DESC, id DESC
LIMIT 1
`

type GetLatestActiveAppVersionParams struct {
	Platform           string `json:"platform"`
	Channel            string `json:"channel"`
	PackageName        string `json:"package_name"`
	CurrentVersionCode int32  `json:"current_version_code"`
}

func (q *Queries) GetLatestActiveAppVersion(ctx context.Context, arg GetLatestActiveAppVersionParams) (AppVersion, error) {
	row := q.db.QueryRow(ctx, getLatestActiveAppVersion,
		arg.Platform,
		arg.Channel,
		arg.PackageName,
		arg.CurrentVersionCode,
	)
	var i AppVersion
	err := row.Scan(
		&i.ID,
		&i.Platform,
		&i.Channel,
		&i.PackageName,
		&i.VersionCode,
		&i.VersionName,
		&i.DownloadUrl,
		&i.Changelog,
		&i.IsForce,
		&i.FileSizeBytes,
		&i.ChecksumSha256,
		&i.Status,
		&i.PublishedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
