// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: ingredient.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countIngredients = `-- name: CountIngredients :one
SELECT COUNT(*) FROM ingredients
WHERE 
  ($1::boolean IS NULL OR is_system = $1)
  AND ($2::text IS NULL OR category = $2)
`

type CountIngredientsParams struct {
	IsSystem pgtype.Bool `json:"is_system"`
	Category pgtype.Text `json:"category"`
}

func (q *Queries) CountIngredients(ctx context.Context, arg CountIngredientsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countIngredients, arg.IsSystem, arg.Category)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createIngredient = `-- name: CreateIngredient :one

INSERT INTO ingredients (
  name,
  is_system,
  category,
  is_allergen,
  created_by
) VALUES (
  $1, $2, $3, $4, $5
) RETURNING id, name, is_system, category, is_allergen, created_by, created_at
`

type CreateIngredientParams struct {
	Name       string      `json:"name"`
	IsSystem   bool        `json:"is_system"`
	Category   pgtype.Text `json:"category"`
	IsAllergen bool        `json:"is_allergen"`
	CreatedBy  pgtype.Int8 `json:"created_by"`
}

// ============================================
// 食材管理查询 (Ingredient Queries)
// ============================================
func (q *Queries) CreateIngredient(ctx context.Context, arg CreateIngredientParams) (Ingredient, error) {
	row := q.db.QueryRow(ctx, createIngredient,
		arg.Name,
		arg.IsSystem,
		arg.Category,
		arg.IsAllergen,
		arg.CreatedBy,
	)
	var i Ingredient
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.IsSystem,
		&i.Category,
		&i.IsAllergen,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const deleteIngredient = `-- name: DeleteIngredient :exec
DELETE FROM ingredients
WHERE id = $1
`

func (q *Queries) DeleteIngredient(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deleteIngredient, id)
	return err
}

const getIngredient = `-- name: GetIngredient :one
SELECT id, name, is_system, category, is_allergen, created_by, created_at FROM ingredients
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetIngredient(ctx context.Context, id int64) (Ingredient, error) {
	row := q.db.QueryRow(ctx, getIngredient, id)
	var i Ingredient
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.IsSystem,
		&i.Category,
		&i.IsAllergen,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const listIngredients = `-- name: ListIngredients :many
SELECT id, name, is_system, category, is_allergen, created_by, created_at FROM ingredients
WHERE 
  ($3::boolean IS NULL OR is_system = $3)
  AND ($4::text IS NULL OR category = $4)
  AND ($5::boolean IS NULL OR is_allergen = $5)
ORDER BY is_system DESC, name ASC
LIMIT $1 OFFSET $2
`

type ListIngredientsParams struct {
	Limit      int32       `json:"limit"`
	Offset     int32       `json:"offset"`
	IsSystem   pgtype.Bool `json:"is_system"`
	Category   pgtype.Text `json:"category"`
	IsAllergen pgtype.Bool `json:"is_allergen"`
}

func (q *Queries) ListIngredients(ctx context.Context, arg ListIngredientsParams) ([]Ingredient, error) {
	rows, err := q.db.Query(ctx, listIngredients,
		arg.Limit,
		arg.Offset,
		arg.IsSystem,
		arg.Category,
		arg.IsAllergen,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Ingredient{}
	for rows.Next() {
		var i Ingredient
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.IsSystem,
			&i.Category,
			&i.IsAllergen,
			&i.CreatedBy,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchIngredients = `-- name: SearchIngredients :many
SELECT id, name, is_system, category, is_allergen, created_by, created_at FROM ingredients
WHERE name ILIKE '%' || $1 || '%'
ORDER BY is_system DESC, name ASC
LIMIT $2 OFFSET $3
`

type SearchIngredientsParams struct {
	Column1 pgtype.Text `json:"column_1"`
	Limit   int32       `json:"limit"`
	Offset  int32       `json:"offset"`
}

func (q *Queries) SearchIngredients(ctx context.Context, arg SearchIngredientsParams) ([]Ingredient, error) {
	rows, err := q.db.Query(ctx, searchIngredients, arg.Column1, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Ingredient{}
	for rows.Next() {
		var i Ingredient
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.IsSystem,
			&i.Category,
			&i.IsAllergen,
			&i.CreatedBy,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateIngredient = `-- name: UpdateIngredient :one
UPDATE ingredients
SET
  name = COALESCE($2, name),
  category = COALESCE($3, category),
  is_allergen = COALESCE($4, is_allergen)
WHERE id = $1
RETURNING id, name, is_system, category, is_allergen, created_by, created_at
`

type UpdateIngredientParams struct {
	ID         int64       `json:"id"`
	Name       pgtype.Text `json:"name"`
	Category   pgtype.Text `json:"category"`
	IsAllergen pgtype.Bool `json:"is_allergen"`
}

func (q *Queries) UpdateIngredient(ctx context.Context, arg UpdateIngredientParams) (Ingredient, error) {
	row := q.db.QueryRow(ctx, updateIngredient,
		arg.ID,
		arg.Name,
		arg.Category,
		arg.IsAllergen,
	)
	var i Ingredient
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.IsSystem,
		&i.Category,
		&i.IsAllergen,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}
