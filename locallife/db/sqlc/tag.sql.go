// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: tag.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createTag = `-- name: CreateTag :one
INSERT INTO tags (
  name,
  type,
  sort_order,
  status
) VALUES (
  $1, $2, $3, $4
)
RETURNING id, name, type, sort_order, status, created_at
`

type CreateTagParams struct {
	Name      string `json:"name"`
	Type      string `json:"type"`
	SortOrder int16  `json:"sort_order"`
	Status    string `json:"status"`
}

func (q *Queries) CreateTag(ctx context.Context, arg CreateTagParams) (Tag, error) {
	row := q.db.QueryRow(ctx, createTag,
		arg.Name,
		arg.Type,
		arg.SortOrder,
		arg.Status,
	)
	var i Tag
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.SortOrder,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const deleteTag = `-- name: DeleteTag :exec
DELETE FROM tags
WHERE id = $1
`

func (q *Queries) DeleteTag(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deleteTag, id)
	return err
}

const getTag = `-- name: GetTag :one
SELECT id, name, type, sort_order, status, created_at FROM tags
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetTag(ctx context.Context, id int64) (Tag, error) {
	row := q.db.QueryRow(ctx, getTag, id)
	var i Tag
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.SortOrder,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const listAllTagsByType = `-- name: ListAllTagsByType :many
SELECT id, name, type, sort_order, status, created_at FROM tags
WHERE type = $1 AND status = 'active'
ORDER BY sort_order ASC, name ASC
`

func (q *Queries) ListAllTagsByType(ctx context.Context, type_ string) ([]Tag, error) {
	rows, err := q.db.Query(ctx, listAllTagsByType, type_)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Tag{}
	for rows.Next() {
		var i Tag
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Type,
			&i.SortOrder,
			&i.Status,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTags = `-- name: ListTags :many
SELECT id, name, type, sort_order, status, created_at FROM tags
WHERE type = $1 AND status = 'active'
ORDER BY sort_order ASC, name ASC
LIMIT $2 OFFSET $3
`

type ListTagsParams struct {
	Type   string `json:"type"`
	Limit  int32  `json:"limit"`
	Offset int32  `json:"offset"`
}

func (q *Queries) ListTags(ctx context.Context, arg ListTagsParams) ([]Tag, error) {
	rows, err := q.db.Query(ctx, listTags, arg.Type, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Tag{}
	for rows.Next() {
		var i Tag
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Type,
			&i.SortOrder,
			&i.Status,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchTags = `-- name: SearchTags :many
SELECT id, name, type, sort_order, status, created_at FROM tags
WHERE type = $1
  AND name ILIKE '%' || $2 || '%'
ORDER BY name
LIMIT $3 OFFSET $4
`

type SearchTagsParams struct {
	Type    string      `json:"type"`
	Column2 pgtype.Text `json:"column_2"`
	Limit   int32       `json:"limit"`
	Offset  int32       `json:"offset"`
}

func (q *Queries) SearchTags(ctx context.Context, arg SearchTagsParams) ([]Tag, error) {
	rows, err := q.db.Query(ctx, searchTags,
		arg.Type,
		arg.Column2,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Tag{}
	for rows.Next() {
		var i Tag
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Type,
			&i.SortOrder,
			&i.Status,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTag = `-- name: UpdateTag :one
UPDATE tags
SET
  name = COALESCE($1, name),
  sort_order = COALESCE($2, sort_order),
  status = COALESCE($3, status)
WHERE id = $4
RETURNING id, name, type, sort_order, status, created_at
`

type UpdateTagParams struct {
	Name      pgtype.Text `json:"name"`
	SortOrder pgtype.Int2 `json:"sort_order"`
	Status    pgtype.Text `json:"status"`
	ID        int64       `json:"id"`
}

func (q *Queries) UpdateTag(ctx context.Context, arg UpdateTagParams) (Tag, error) {
	row := q.db.QueryRow(ctx, updateTag,
		arg.Name,
		arg.SortOrder,
		arg.Status,
		arg.ID,
	)
	var i Tag
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.SortOrder,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}
