// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: rider_deposit.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createRiderDeposit = `-- name: CreateRiderDeposit :one
INSERT INTO rider_deposits (
    rider_id,
    amount,
    type,
    related_order_id,
    balance_after,
    remark
) VALUES (
    $1, $2, $3, $4, $5, $6
) RETURNING id, rider_id, amount, type, related_order_id, balance_after, remark, created_at
`

type CreateRiderDepositParams struct {
	RiderID        int64       `json:"rider_id"`
	Amount         int64       `json:"amount"`
	Type           string      `json:"type"`
	RelatedOrderID pgtype.Int8 `json:"related_order_id"`
	BalanceAfter   int64       `json:"balance_after"`
	Remark         pgtype.Text `json:"remark"`
}

func (q *Queries) CreateRiderDeposit(ctx context.Context, arg CreateRiderDepositParams) (RiderDeposit, error) {
	row := q.db.QueryRow(ctx, createRiderDeposit,
		arg.RiderID,
		arg.Amount,
		arg.Type,
		arg.RelatedOrderID,
		arg.BalanceAfter,
		arg.Remark,
	)
	var i RiderDeposit
	err := row.Scan(
		&i.ID,
		&i.RiderID,
		&i.Amount,
		&i.Type,
		&i.RelatedOrderID,
		&i.BalanceAfter,
		&i.Remark,
		&i.CreatedAt,
	)
	return i, err
}

const getRiderDeposit = `-- name: GetRiderDeposit :one
SELECT id, rider_id, amount, type, related_order_id, balance_after, remark, created_at FROM rider_deposits
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetRiderDeposit(ctx context.Context, id int64) (RiderDeposit, error) {
	row := q.db.QueryRow(ctx, getRiderDeposit, id)
	var i RiderDeposit
	err := row.Scan(
		&i.ID,
		&i.RiderID,
		&i.Amount,
		&i.Type,
		&i.RelatedOrderID,
		&i.BalanceAfter,
		&i.Remark,
		&i.CreatedAt,
	)
	return i, err
}

const getRiderDepositStats = `-- name: GetRiderDepositStats :one
SELECT 
    COALESCE(SUM(CASE WHEN type = 'deposit' THEN amount ELSE 0 END), 0) AS total_deposit,
    COALESCE(SUM(CASE WHEN type = 'withdraw' THEN amount ELSE 0 END), 0) AS total_withdraw,
    COALESCE(SUM(CASE WHEN type = 'deduct' THEN amount ELSE 0 END), 0) AS total_deduct
FROM rider_deposits
WHERE rider_id = $1
`

type GetRiderDepositStatsRow struct {
	TotalDeposit  interface{} `json:"total_deposit"`
	TotalWithdraw interface{} `json:"total_withdraw"`
	TotalDeduct   interface{} `json:"total_deduct"`
}

func (q *Queries) GetRiderDepositStats(ctx context.Context, riderID int64) (GetRiderDepositStatsRow, error) {
	row := q.db.QueryRow(ctx, getRiderDepositStats, riderID)
	var i GetRiderDepositStatsRow
	err := row.Scan(&i.TotalDeposit, &i.TotalWithdraw, &i.TotalDeduct)
	return i, err
}

const listRiderDeposits = `-- name: ListRiderDeposits :many
SELECT id, rider_id, amount, type, related_order_id, balance_after, remark, created_at FROM rider_deposits
WHERE rider_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type ListRiderDepositsParams struct {
	RiderID int64 `json:"rider_id"`
	Limit   int32 `json:"limit"`
	Offset  int32 `json:"offset"`
}

func (q *Queries) ListRiderDeposits(ctx context.Context, arg ListRiderDepositsParams) ([]RiderDeposit, error) {
	rows, err := q.db.Query(ctx, listRiderDeposits, arg.RiderID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []RiderDeposit{}
	for rows.Next() {
		var i RiderDeposit
		if err := rows.Scan(
			&i.ID,
			&i.RiderID,
			&i.Amount,
			&i.Type,
			&i.RelatedOrderID,
			&i.BalanceAfter,
			&i.Remark,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRiderDepositsByType = `-- name: ListRiderDepositsByType :many
SELECT id, rider_id, amount, type, related_order_id, balance_after, remark, created_at FROM rider_deposits
WHERE rider_id = $1 AND type = $2
ORDER BY created_at DESC
LIMIT $3 OFFSET $4
`

type ListRiderDepositsByTypeParams struct {
	RiderID int64  `json:"rider_id"`
	Type    string `json:"type"`
	Limit   int32  `json:"limit"`
	Offset  int32  `json:"offset"`
}

func (q *Queries) ListRiderDepositsByType(ctx context.Context, arg ListRiderDepositsByTypeParams) ([]RiderDeposit, error) {
	rows, err := q.db.Query(ctx, listRiderDepositsByType,
		arg.RiderID,
		arg.Type,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []RiderDeposit{}
	for rows.Next() {
		var i RiderDeposit
		if err := rows.Scan(
			&i.ID,
			&i.RiderID,
			&i.Amount,
			&i.Type,
			&i.RelatedOrderID,
			&i.BalanceAfter,
			&i.Remark,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
